import { useEffect, useState, type ChangeEvent, type ReactNode } from "react";
import {
  Activity,
  AlertTriangle,
  ArrowDownRight,
  ArrowUpRight,
  Eye,
  EyeOff,
  BarChart3,
  BatteryCharging,
  Bell,
  BookOpen,
  Building2,
  CalendarDays,
  ClipboardCheck,
  Check,
  ChevronRight,
  CircleHelp,
  CloudSun,
  Database,
  Download,
  FileBarChart,
  FileCheck2,
  Filter,
  Gauge,
  GitBranch,
  Grid2X2,
  Info,
  LampDesk,
  Leaf,
  LineChart,
  Menu,
  MessageCircle,
  PanelsTopLeft,
  Play,
  Plus,
  RefreshCw,
  Search,
  Settings2,
  ShieldCheck,
  SlidersHorizontal,
  Sparkles,
  SunMedium,
  Target,
  ThermometerSun,
  Upload,
  UserRound,
  Users,
  Wind,
  X,
  Zap,
} from "lucide-react";
import {
  Area,
  AreaChart,
  Bar,
  BarChart,
  CartesianGrid,
  Cell,
  ComposedChart,
  Line,
  LineChart as RechartsLineChart,
  Pie,
  PieChart,
  ResponsiveContainer,
  Tooltip,
  XAxis,
  YAxis,
} from "recharts";
import { Toaster, toast } from "sonner";
import { trpc } from "@/lib/trpc";

const buildings = [
  { name: "Academic Block", short: "AB", energy: 1240, share: 22, status: "Normal", tone: "teal", occupancy: 68, hours: "08:00–18:00", ac: 42, lighting: 26, equipment: 32, baseline: 1180, predicted: 1290, trend: [28, 32, 30, 36, 34, 40, 38] },
  { name: "Computer Labs", short: "CL", energy: 1850, share: 33, status: "Attention", tone: "amber", occupancy: 54, hours: "08:00–20:00", ac: 38, lighting: 18, equipment: 44, baseline: 1460, predicted: 1920, trend: [45, 50, 48, 62, 58, 72, 68] },
  { name: "Hostel", short: "HS", energy: 1540, share: 27, status: "Normal", tone: "teal", occupancy: 81, hours: "06:00–23:00", ac: 35, lighting: 30, equipment: 35, baseline: 1490, predicted: 1570, trend: [36, 38, 44, 42, 47, 45, 49] },
  { name: "Library", short: "LB", energy: 580, share: 10, status: "Normal", tone: "blue", occupancy: 46, hours: "09:00–21:00", ac: 41, lighting: 36, equipment: 23, baseline: 560, predicted: 600, trend: [18, 20, 19, 24, 23, 25, 24] },
  { name: "Canteen", short: "CN", energy: 320, share: 6, status: "Normal", tone: "blue", occupancy: 39, hours: "07:00–19:00", ac: 18, lighting: 26, equipment: 56, baseline: 300, predicted: 340, trend: [12, 16, 14, 18, 17, 20, 19] },
  { name: "Auditorium", short: "AU", energy: 170, share: 2, status: "Low use", tone: "slate", occupancy: 18, hours: "10:00–17:00", ac: 44, lighting: 32, equipment: 24, baseline: 180, predicted: 155, trend: [14, 12, 11, 10, 13, 9, 8] },
];
const trendData = [
  { label: "Mon", actual: 5320, prediction: 5180, peak: 780 },
  { label: "Tue", actual: 5780, prediction: 5610, peak: 835 },
  { label: "Wed", actual: 6120, prediction: 5980, peak: 910 },
  { label: "Thu", actual: 5940, prediction: 6020, peak: 890 },
  { label: "Fri", actual: 6410, prediction: 6260, peak: 960 },
  { label: "Sat", actual: 4660, prediction: 4780, peak: 710 },
  { label: "Sun", actual: 3310, prediction: 3520, peak: 520 },
];

const hourlyData = [
  { time: "06", value: 120 }, { time: "08", value: 390 }, { time: "10", value: 620 }, { time: "12", value: 710 },
  { time: "14", value: 920 }, { time: "16", value: 880 }, { time: "18", value: 650 }, { time: "20", value: 430 }, { time: "22", value: 220 },
];

const predictionData = [
  { day: "Today", actual: 6010, model: 5980, lower: 5630, upper: 6330 },
  { day: "Tomorrow", actual: null, model: 6280, lower: 5900, upper: 6660 },
  { day: "Thu", actual: null, model: 6410, lower: 5990, upper: 6830 },
  { day: "Fri", actual: null, model: 6530, lower: 6120, upper: 6940 },
  { day: "Sat", actual: null, model: 4720, lower: 4430, upper: 5010 },
  { day: "Sun", actual: null, model: 3490, lower: 3170, upper: 3810 },
];

const anomalies = [
  { id: "lab-2", building: "Computer Labs", place: "Lab 2", when: "Today · 14:00", observed: 145, baseline: "80–100", deviation: "+45%", severity: "High", factor: "cooling demand / extended equipment runtime" },
  { id: "hostel-a", building: "Hostel", place: "North wing", when: "Yesterday · 21:00", observed: 98, baseline: "70–88", deviation: "+11%", severity: "Medium", factor: "increased occupancy / operating hours" },
  { id: "academic-3", building: "Academic Block", place: "Classrooms 3–4", when: "Mon · 10:00", observed: 42, baseline: "55–70", deviation: "−24%", severity: "Low", factor: "lower occupancy / data quality" },
];

const knowledgeAnswers: Record<string, { title: string; body: string; type: string }> = {
  most: { title: "Computer Labs is the highest modeled consumer", body: "In the current demo dataset, Computer Labs accounts for 1,850 kWh/day, or about 33% of modeled campus consumption. This is data-derived from synthetic records, not a real meter reading.", type: "Data-derived answer" },
  tomorrow: { title: "Tomorrow’s demand is estimated at 6,280 kWh", body: "The local baseline model estimates 6,280 kWh for tomorrow with a modeled range of 5,900–6,660 kWh. The afternoon is the likely high-demand window. This is a model prediction, not a guarantee.", type: "Model prediction" },
  lab: { title: "Lab 2 was flagged above its modeled baseline", body: "The observed demo value of 145 kWh was compared with a modeled range of 80–100 kWh for the same building and time period. Possible contributing factors include cooling demand, extended operating hours, equipment usage, or a data anomaly. The exact physical cause is not known.", type: "Anomaly explanation" },
  lighting: { title: "A 20% lighting reduction is a scenario estimate", body: "With the current assumptions, reducing modeled lighting consumption by 20% lowers campus energy by approximately 2.6% before any solar contribution. This is an adjustable estimate and depends on how much lighting load is actually controllable.", type: "Scenario estimate" },
  sdg: { title: "The project supports SDG 7 through decision support", body: "CampusEnergy Twin helps users understand energy demand, test efficiency and renewable scenarios, and surface transparent assumptions. It does not claim to achieve SDG targets or replace measured verification.", type: "Knowledge-base explanation" },
};

const navGroups = [
  { label: "Workspace", items: [
    ["dashboard", "Dashboard", PanelsTopLeft], ["twin", "Digital Campus Twin", Grid2X2], ["monitoring", "Energy Monitoring", Activity], ["predictions", "AI Predictions", LineChart], ["anomalies", "Anomaly Detection", AlertTriangle],
  ]},
  { label: "Decision support", items: [
    ["whatif", "What-If Simulator", SlidersHorizontal], ["solar", "Renewable Planning", SunMedium], ["peak", "Peak Demand", Gauge], ["recommendations", "Recommendations", Sparkles], ["action", "Action Center", ClipboardCheck],
  ]},
  { label: "Intelligence", items: [
    ["assistant", "EnergyAI Assistant", MessageCircle], ["impact", "Impact Dashboard", Leaf], ["reports", "Reports", FileBarChart],
  ]},
  { label: "Data", items: [
    ["sources", "Data Sources", GitBranch], ["data", "Data Management", Database],
  ]},
  { label: "Administration", items: [
    ["users", "User Management", Users], ["responsible", "Responsible AI", ShieldCheck], ["about", "About Project", BookOpen], ["settings", "Settings", Settings2],
  ]},
] as const;

type Section = typeof navGroups[number]["items"][number][0] | "landing";
type DataSource = "demo" | "uploaded" | "simulated" | "api";
type Role = "Facility Manager" | "Sustainability Manager" | "Energy Analyst" | "Administrator" | "Demo / Evaluator";

type Scenario = { name: string; tag: string; led: number; ac: number; schedule: number; equipment: number; solar: number; energy: number; cost: number; co2: number };
type ScenarioControls = { led: number; ac: number; schedule: number; equipment: number; solar: number; roof: number };

const initialScenarios: Scenario[] = [
  { name: "Current campus", tag: "Baseline", led: 0, ac: 0, schedule: 0, equipment: 0, solar: 0, energy: 5810, cost: 522.9, co2: 3.08 },
  { name: "LED upgrade", tag: "Efficiency", led: 30, ac: 0, schedule: 0, equipment: 0, solar: 0, energy: 5660, cost: 509.4, co2: 3.0 },
  { name: "Schedule optimization", tag: "Load shifting", led: 0, ac: 0, schedule: 12, equipment: 0, solar: 0, energy: 5740, cost: 516.6, co2: 3.04 },
  { name: "Combined strategy", tag: "Efficiency + solar", led: 30, ac: 8, schedule: 12, equipment: 10, solar: 1600, energy: 3980, cost: 358.2, co2: 2.11 },
];

function money(value: number) { return `₹${value.toLocaleString("en-IN", { maximumFractionDigits: 0 })}`; }
function num(value: number) { return value.toLocaleString("en-IN", { maximumFractionDigits: 0 }); }
function pct(value: number) { return `${value.toFixed(1)}%`; }

function StatusPill({ children, tone = "teal" }: { children: ReactNode; tone?: "teal" | "amber" | "slate" | "blue" | "red" }) {
  return <span className={`pill pill-${tone}`}><span className="pill-dot" />{children}</span>;
}

function Card({ children, className = "" }: { children: ReactNode; className?: string }) {
  return <div className={`card ${className}`}>{children}</div>;
}

function SectionTitle({ eyebrow, title, copy, action }: { eyebrow?: string; title: string; copy?: string; action?: ReactNode }) {
  return <div className="section-title"><div><div className="eyebrow">{eyebrow}</div><h1>{title}</h1>{copy && <p>{copy}</p>}</div>{action}</div>;
}

function KpiCard({ label, value, detail, icon: Icon, accent, trend, positive = true }: { label: string; value: string; detail: string; icon: typeof Zap; accent: string; trend?: string; positive?: boolean }) {
  return <Card className="kpi-card"><div className={`kpi-icon ${accent}`}><Icon size={18} /></div><div className="kpi-label">{label}</div><div className="kpi-value">{value}</div><div className="kpi-detail">{trend && <span className={positive ? "trend-up" : "trend-down"}>{positive ? <ArrowUpRight size={13} /> : <ArrowDownRight size={13} />}{trend}</span>} {detail}</div></Card>;
}

function ChartTooltip({ active, payload, label }: { active?: boolean; payload?: { value: number; name: string; color?: string }[]; label?: string }) {
  if (!active || !payload?.length) return null;
  return <div className="chart-tooltip"><strong>{label}</strong>{payload.map((item) => <div key={item.name}><span style={{ color: item.color }}>{item.name}</span><b>{num(item.value)} kWh</b></div>)}</div>;
}

function Spark({ data, color = "#13b8a6" }: { data: number[]; color?: string }) {
  const values = data.map((value, index) => ({ index, value }));
  return <div className="spark"><ResponsiveContainer width="100%" height="100%"><RechartsLineChart data={values}><Line type="monotone" dataKey="value" stroke={color} strokeWidth={2.5} dot={false} /></RechartsLineChart></ResponsiveContainer></div>;
}

function MiniBar({ value, color = "#13b8a6" }: { value: number; color?: string }) {
  return <div className="mini-bar"><span style={{ width: `${Math.min(value, 100)}%`, background: color }} /></div>;
}

type Profile = { name: string; email: string; institution: string; role: Role };
type ApiConfig = { endpoint: string; key: string; building: string; meter: string; interval: number };
type PersistedCsvRow = { date: string; time?: string; building: string; occupancy?: string; temperature?: string; acUsage?: string; lightingUsage?: string; equipmentUsage?: string; energyConsumption: string };
const sourceLabels: Record<DataSource, string> = { demo: "DEMO • SYNTHETIC DATA", uploaded: "UPLOADED • CAMPUS DATA", simulated: "SIMULATED • SMART METER", api: "CONNECTED • API DATA" };
const allSections: Section[] = ["dashboard", "twin", "monitoring", "predictions", "anomalies", "whatif", "solar", "peak", "recommendations", "action", "assistant", "impact", "reports", "sources", "data", "users", "responsible", "about", "settings"];
const roleAccess: Record<Role, Section[]> = { "Facility Manager": ["dashboard", "twin", "monitoring", "predictions", "anomalies", "action", "peak", "recommendations", "whatif", "assistant", "reports", "sources", "data", "responsible", "settings"], "Sustainability Manager": ["dashboard", "twin", "monitoring", "whatif", "solar", "impact", "recommendations", "reports", "assistant", "responsible", "sources"], "Energy Analyst": ["dashboard", "monitoring", "predictions", "anomalies", "twin", "whatif", "peak", "data", "assistant", "reports", "sources"], "Administrator": allSections, "Demo / Evaluator": allSections };

function App() {
  const [authenticated, setAuthenticated] = useState(false);
  const [authView, setAuthView] = useState<"login" | "signup">("login");
  const [profile, setProfile] = useState<Profile>({ name: "Demo user", email: "demo@campusenergy.twin", institution: "Northbridge Institute", role: "Facility Manager" });
  const [activeSource, setActiveSource] = useState<DataSource>("demo");
  const [meterRunning, setMeterRunning] = useState(false);
  const [meterReading, setMeterReading] = useState({ building: "Computer Labs", meter: "SIM-CL-01", energy: 2.8, power: 3.4, voltage: 231, current: 14.7, timestamp: "10:01:00" });
  const [section, setSection] = useState<Section>("dashboard");
  const role = profile.role;
  const isReadOnly = role === "Demo / Evaluator";
  const [mobileOpen, setMobileOpen] = useState(false);
  const [selectedBuilding, setSelectedBuilding] = useState(buildings[1]);
  const [scenario, setScenario] = useState<ScenarioControls>({ led: 30, ac: 8, schedule: 12, equipment: 10, solar: 0, roof: 3600 });
  const [scenarios, setScenarios] = useState<Scenario[]>(initialScenarios);
  const [reviewed, setReviewed] = useState<string[]>([]);
  const [assistantInput, setAssistantInput] = useState("");
  const [assistantMessages, setAssistantMessages] = useState<{ from: "user" | "ai"; text: string; type?: string }[]>([
    { from: "ai", text: "Welcome to EnergyAI. Ask about campus data, forecasts, anomalies, scenarios, or SDG 7. I’ll separate data-derived answers from model predictions and estimates.", type: "Knowledge-base explanation" },
  ]);
  const [csvProgress, setCsvProgress] = useState(0);
  const [csvResult, setCsvResult] = useState<{ name: string; rows: number; buildings: number; missing: number; invalid: number; duplicates: number; valid: boolean; dateRange: string; totalEnergy: number; errors: { row: number; message: string; raw: string }[] } | null>(null);
  const [apiConfig, setApiConfig] = useState<ApiConfig>({ endpoint: "", key: "", building: "", meter: "", interval: 60 });
  const [parsedCsvRows, setParsedCsvRows] = useState<PersistedCsvRow[]>([]);
  const workspaceQuery = trpc.data.workspace.useQuery(undefined, { retry: false, refetchOnWindowFocus: false });
  const activateSourceMutation = trpc.data.activateSource.useMutation();
  const importDatasetMutation = trpc.data.importDataset.useMutation();
  const saveApiConfigMutation = trpc.data.saveApiConfig.useMutation();
  const [settings, setSettings] = useState({ campus: "Northbridge Institute", tariff: 9, emissions: 0.53 });
  const [filterBuilding, setFilterBuilding] = useState("All buildings");

  useEffect(() => {
    const persisted = workspaceQuery.data;
    if (!persisted) return;
    setActiveSource(persisted.activeSource as DataSource);
    setApiConfig((current) => ({ ...current, ...persisted.apiConfig, key: current.key }));
    if (persisted.activeDataset && !csvResult) {
      setCsvResult({ name: persisted.activeDataset.name, rows: persisted.activeDataset.rows, buildings: persisted.activeDataset.buildings, missing: 0, invalid: 0, duplicates: 0, valid: true, dateRange: persisted.activeDataset.dateRange, totalEnergy: persisted.activeDataset.totalEnergy, errors: [] });
    }
  }, [workspaceQuery.data]);

  useEffect(() => {
    if (!meterRunning) return;
    const timer = window.setInterval(() => setMeterReading((prev) => ({ ...prev, energy: Number((2.6 + Math.random() * .7).toFixed(1)), power: Number((3.1 + Math.random() * .8).toFixed(1)), timestamp: new Date().toLocaleTimeString([], { hour: "2-digit", minute: "2-digit", second: "2-digit" }) })), 2200);
    return () => window.clearInterval(timer);
  }, [meterRunning]);

  const demoBaseline = 5810;
  const baseline = activeSource === "uploaded" && csvResult?.valid ? csvResult.totalEnergy : demoBaseline;
  const simulatedEnergy = Math.max(0, baseline - scenario.led * 5 - scenario.ac * 3 - scenario.schedule * 4 - scenario.equipment * 3 - scenario.solar);
  const simulatedCost = simulatedEnergy * settings.tariff / 100;
  const simulatedCo2 = simulatedEnergy * settings.emissions / 1000;
  const energySaved = baseline - simulatedEnergy;
  const solarContribution = Math.min(100, (scenario.solar / baseline) * 100);

  const currentPage = section === "landing" ? "Start here" : ({
    dashboard: "Dashboard", twin: "Digital Campus Twin", monitoring: "Energy Monitoring", predictions: "AI Predictions", anomalies: "Anomaly Detection",
    whatif: "What-If Simulator", solar: "Renewable Planning", peak: "Peak Demand", recommendations: "Recommendations", action: "Action Center", assistant: "EnergyAI Assistant", impact: "Impact Dashboard", reports: "Reports",
    sources: "Data Sources", data: "Data Management", users: "User Management", responsible: "Responsible AI", about: "About Project", settings: "Settings",
  } as Record<string, string>)[section] ?? "Dashboard";

  const go = (next: Section) => {
    if (!roleAccess[role].includes(next)) { toast.error("Access restricted", { description: `${role} does not have access to this workspace area.` }); return; }
    setSection(next); setMobileOpen(false); window.scrollTo({ top: 0, behavior: "smooth" });
  };
  const signIn = (nextProfile: Profile) => { setProfile(nextProfile); setAuthenticated(true); setSection("dashboard"); toast.success("Welcome back", { description: `${nextProfile.name} · ${nextProfile.role}` }); };
  const sourceLabel = sourceLabels[activeSource];

  const saveScenario = () => {
    if (isReadOnly) { toast.info("Demo / Evaluator is read-only", { description: "You can explore scenarios but cannot save changes." }); return; }
    const next: Scenario = { name: `Custom scenario ${scenarios.length - 3}`, tag: "User estimate", led: scenario.led, ac: scenario.ac, schedule: scenario.schedule, equipment: scenario.equipment, solar: scenario.solar, energy: simulatedEnergy, cost: simulatedCost, co2: simulatedCo2 };
    setScenarios((prev) => [...prev, next]);
    toast.success("Scenario saved", { description: "Assumptions are visible in the comparison table." });
  };

  const handleCsv = (event: ChangeEvent<HTMLInputElement>) => {
    if (isReadOnly) { toast.info("Demo / Evaluator is read-only", { description: "Upload is disabled in evaluator mode." }); return; }
    const file = event.target.files?.[0];
    if (!file) return;
    if (!file.name.toLowerCase().endsWith(".csv")) { toast.error("Unsupported file type", { description: "Please choose a .csv file." }); return; }
    setCsvProgress(0); setCsvResult(null);
    const reader = new FileReader();
    reader.onprogress = (progressEvent) => { if (progressEvent.lengthComputable) setCsvProgress(Math.round((progressEvent.loaded / progressEvent.total) * 70)); };
    reader.onerror = () => { setCsvProgress(0); toast.error("Upload failed", { description: "The browser could not read this file. Try exporting it again as UTF-8 CSV." }); };
    reader.onload = () => {
      setCsvProgress(82);
      const text = String(reader.result || "");
      const lines = text.split(/\r?\n/).map((line) => line.trim()).filter(Boolean);
      if (!lines.length) { setCsvProgress(100); toast.error("Empty CSV", { description: "The file has no header or data rows." }); return; }
      const headers = lines[0].split(",").map((v) => v.trim().toLowerCase().replace(/\s+/g, "_").replace(/[()]/g, ""));
      const required = ["date", "building", "energy_consumption"];
      const missing = required.filter((column) => !headers.includes(column)).length;
      const rows = lines.slice(1).map((line) => line.split(",").map((v) => v.trim()));
      const signatures = rows.map((row) => row.join("|"));
      const duplicateIndexes = new Set<number>();
      const firstSeen = new Map<string, number>();
      signatures.forEach((signature, index) => { if (firstSeen.has(signature)) { duplicateIndexes.add(index); duplicateIndexes.add(firstSeen.get(signature)!); } else firstSeen.set(signature, index); });
      const duplicates = duplicateIndexes.size;
      const buildingIndex = headers.indexOf("building");
      const energyIndex = headers.indexOf("energy_consumption");
      const dateIndex = headers.indexOf("date");
      const detectedBuildings = new Set(rows.map((row) => row[buildingIndex]).filter(Boolean));
      const errors: { row: number; message: string; raw: string }[] = [];
      rows.forEach((row, index) => {
        const rowNumber = index + 2;
        if (missing) { errors.push({ row: rowNumber, message: `Missing required column${missing > 1 ? "s" : ""}: ${required.filter((column) => !headers.includes(column)).join(", ")}`, raw: lines[index + 1] }); return; }
        if (!row[dateIndex]) errors.push({ row: rowNumber, message: "Date is empty", raw: lines[index + 1] });
        if (!row[buildingIndex]) errors.push({ row: rowNumber, message: "Building is empty", raw: lines[index + 1] });
        if (!row[energyIndex]) errors.push({ row: rowNumber, message: "Energy consumption is empty", raw: lines[index + 1] });
        else if (Number.isNaN(Number(row[energyIndex]))) errors.push({ row: rowNumber, message: `Energy consumption is not numeric: “${row[energyIndex]}”`, raw: lines[index + 1] });
        else if (Number(row[energyIndex]) < 0) errors.push({ row: rowNumber, message: "Energy consumption cannot be negative", raw: lines[index + 1] });
        if (duplicateIndexes.has(index)) errors.push({ row: rowNumber, message: "Duplicate record matches another row", raw: lines[index + 1] });
      });
      const validErrors = errors.filter((error) => !error.message.startsWith("Duplicate"));
      const invalid = validErrors.length;
      const dateValues = rows.map((row) => row[dateIndex]).filter(Boolean).sort();
      const dateRange = dateValues.length ? `${dateValues[0]} → ${dateValues[dateValues.length - 1]}` : "Not available";
      const totalEnergy = rows.reduce((sum, row) => sum + (Number(row[energyIndex]) || 0), 0);
      const valueAt = (row: string[], names: string[]) => { const index = names.map((name) => headers.indexOf(name)).find((candidate) => candidate >= 0); return index === undefined ? undefined : row[index] || undefined; };
      const parsedRows: PersistedCsvRow[] = rows.map((row) => ({ date: valueAt(row, ["date"]) || "", time: valueAt(row, ["time"]), building: valueAt(row, ["building"]) || "", occupancy: valueAt(row, ["occupancy"]), temperature: valueAt(row, ["temperature"]), acUsage: valueAt(row, ["ac_usage", "acusage"]), lightingUsage: valueAt(row, ["lighting_usage", "lightingusage"]), equipmentUsage: valueAt(row, ["equipment_usage", "equipmentusage"]), energyConsumption: valueAt(row, ["energy_consumption", "energyconsumption"]) || "" }));
      const result = { name: file.name, rows: rows.length, buildings: detectedBuildings.size, missing, invalid, duplicates, dateRange, totalEnergy, errors: errors.slice(0, 50), valid: missing === 0 && rows.length > 0 && errors.length === 0 };
      setParsedCsvRows(result.valid ? parsedRows : []);
      setCsvResult(result); setCsvProgress(100);
      toast[result.valid ? "success" : "error"](result.valid ? "Dataset validated" : "Dataset needs attention", { description: result.valid ? `${rows.length} rows ready for analysis.` : `${errors.length} row-level issue${errors.length === 1 ? "" : "s"} found. Review the diagnostics before importing.` });
    };
    reader.readAsText(file);
  };

  const downloadTemplate = () => { const header = "Date,Time,Building,Room,Occupancy,Temperature,AC Usage,Lighting Usage,Equipment Usage,Energy Consumption\n2026-09-01,14:00,Computer Labs,Lab 2,58,29,38,18,44,145\n2026-09-01,15:00,Academic Block,Room 101,72,27,42,26,32,88\n2026-09-01,16:00,Hostel,North wing,81,26,35,30,35,96\n\nExample structure only."; const blob = new Blob([header], { type: "text/csv" }); const url = URL.createObjectURL(blob); const link = document.createElement("a"); link.href = url; link.download = "campus-energy-template.csv"; link.click(); URL.revokeObjectURL(url); toast.success("Template downloaded"); };
  const switchSource = (next: DataSource) => {
    if (isReadOnly && next === "api") { toast.info("Evaluator mode", { description: "External API configuration is read-only in Demo / Evaluator mode." }); return; }
    if (next === "uploaded") {
      if (!csvResult?.valid || !parsedCsvRows.length) { toast.error("Import blocked", { description: "Upload a valid CSV before importing it into the shared analytics system." }); return; }
      importDatasetMutation.mutate({ name: csvResult.name, buildingCount: csvResult.buildings, totalEnergy: csvResult.totalEnergy, dateRange: csvResult.dateRange, rows: parsedCsvRows }, { onSuccess: () => { setActiveSource("uploaded"); toast.success("Dataset persisted", { description: "Uploaded readings are now stored and used by the shared workspace." }); }, onError: () => toast.error("Dataset persistence failed", { description: "The CSV remains available locally; retry when the database is reachable." }) });
      return;
    }
    if (next === "api") { toast.info("External API integration ready", { description: "Connection required — no live API data is connected in this prototype." }); return; }
    setActiveSource(next);
    activateSourceMutation.mutate({ source: next }, { onError: () => toast.error("Source state not persisted", { description: "The source changed locally, but the database could not be reached." }) });
    toast.success(`Data source switched: ${sourceLabels[next]}`, { description: next === "simulated" ? "Generated readings now feed the shared prototype source state." : "The workspace now uses this source label consistently." });
  };
  const toggleMeter = () => { if (isReadOnly && !meterRunning) { toast.info("Evaluator mode", { description: "You can view the simulation but cannot start a stream." }); return; } setMeterRunning((running) => !running); setActiveSource("simulated"); };

  const askAssistant = () => {
    const question = assistantInput.trim();
    if (!question) return;
    const lower = question.toLowerCase();
    let answer = knowledgeAnswers.sdg;
    if (lower.includes("most") || lower.includes("highest") || lower.includes("consum")) answer = knowledgeAnswers.most;
    else if (lower.includes("tomorrow") || lower.includes("predict")) answer = knowledgeAnswers.tomorrow;
    else if (lower.includes("lab") || lower.includes("flag") || lower.includes("anomal")) answer = knowledgeAnswers.lab;
    else if (lower.includes("light") || lower.includes("20%") || lower.includes("scenario") || lower.includes("simulat")) answer = knowledgeAnswers.lighting;
    setAssistantMessages((messages) => [...messages, { from: "user", text: question }, { from: "ai", text: `${answer.title}\n\n${answer.body}`, type: answer.type }]);
    setAssistantInput("");
  };

  const downloadReport = () => {
    const report = `CampusEnergy Twin — Sustainability Report\n\nCampus: ${settings.campus}\nData Source: ${sourceLabel}\nReporting period: Last 7 modeled days\n\nCAMPUS OVERVIEW\nTotal modeled energy: ${num(baseline)} kWh/day\nEstimated cost: ${money(baseline * settings.tariff / 100)} / day\nEstimated CO2e: ${(baseline * settings.emissions / 1000).toFixed(2)} tCO2e / day\n\nPEAK DEMAND\nHistorical peak: 960 kW\nPredicted high-demand window: 14:00–16:00\n\nANOMALIES\n${anomalies.length} patterns flagged for human review. Exact physical causes are not inferred.\n\nSIMULATED SCENARIO\nEnergy estimate: ${num(simulatedEnergy)} kWh/day\nEstimated reduction: ${num(energySaved)} kWh/day\nEstimated cost difference: ${money((baseline - simulatedEnergy) * settings.tariff / 100)} / day\nEstimated CO2e difference: ${((baseline - simulatedEnergy) * settings.emissions / 1000).toFixed(2)} tCO2e / day\n\nASSUMPTIONS\nElectricity tariff: ₹${settings.tariff}/kWh\nEmissions factor: ${settings.emissions} kgCO2e/kWh\nAll values are demo, estimated, simulated, or model-predicted unless a real dataset is uploaded.\n\nRESPONSIBLE AI\nRecommendations support decisions; they do not control equipment or guarantee savings. Human verification and real-world measurement are required.\n`;
    const blob = new Blob([report], { type: "text/plain;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const link = document.createElement("a"); link.href = url; link.download = "campus-energy-twin-report.txt"; link.click(); URL.revokeObjectURL(url);
    toast.success("Report exported", { description: "A text report with assumptions and limitations was downloaded." });
  };

  if (!authenticated) return <AuthPage view={authView} setView={setAuthView} onAuth={signIn} />;

  return <>
    <Toaster position="top-right" richColors />
    <div className="app-shell">
      <aside className={`sidebar ${mobileOpen ? "mobile-open" : ""}`}>
        <div className="brand" onClick={() => go("landing")} role="button" tabIndex={0}><div className="brand-mark"><Zap size={18} fill="currentColor" /></div><div><strong>CampusEnergy</strong><span>Twin / 01</span></div></div>
        <div className="side-status"><span className="live-dot" /> DEMO MODE <span className="side-status-muted">• SYNTHETIC DATA</span></div>
        <nav className="nav-list">
          {navGroups.map((group) => <div className="nav-group" key={group.label}><div className="nav-label">{group.label}</div>{group.items.map(([id, label, Icon]) => <button key={id} className={`nav-item ${section === id ? "active" : ""}`} onClick={() => go(id)}><Icon size={17} strokeWidth={section === id ? 2.4 : 1.8} /><span>{label}</span>{id === "anomalies" && <span className="nav-count">2</span>}</button>)}</div>)}
        </nav>
        <div className="sidebar-bottom"><div className="profile-card"><strong>{profile.name}</strong><span>{profile.role} · {profile.institution}</span><button onClick={() => { setAuthenticated(false); setAuthView("login"); }}>Logout</button></div><div className="help-card"><CircleHelp size={16} /><div><strong>Need a primer?</strong><span>Explore the project brief</span></div><ChevronRight size={15} /></div><div className="user-chip"><div className="avatar">FM</div><div><strong>Demo user</strong><span>{role}</span></div><Settings2 size={15} /></div></div>
      </aside>
      {mobileOpen && <div className="mobile-scrim" onClick={() => setMobileOpen(false)} />}
      <main className="main-shell">
        <header className="topbar"><button className="mobile-menu" onClick={() => setMobileOpen(true)}><Menu size={20} /></button><div className="breadcrumbs"><span>Campus workspace</span><ChevronRight size={13} /><strong>{currentPage}</strong></div><div className="top-actions"><button className="demo-badge" onClick={() => go("sources")}><span className="live-dot" /> {sourceLabel}</button><button className="icon-button" title="Notifications" onClick={() => toast("No urgent alerts", { description: "Two patterns are waiting for human review." })}><Bell size={18} /><span className="notification-dot" /></button><div className="role-select"><UserRound size={15} /><select value={role} onChange={(e) => { const nextRole = e.target.value as Role; setProfile({ ...profile, role: nextRole }); if (!roleAccess[nextRole].includes(section)) setSection("dashboard"); }} aria-label="Select workspace role"><option>Facility Manager</option><option>Sustainability Manager</option><option>Energy Analyst</option><option>Administrator</option><option>Demo / Evaluator</option></select></div></div></header>
        <div className="page-content">
          {section === "landing" && <LandingPage go={go} />}
          {section === "dashboard" && <DashboardPage go={go} role={role} sourceLabel={sourceLabel} baseline={baseline} />}
          {section === "twin" && <TwinPage selected={selectedBuilding} onSelect={setSelectedBuilding} />}
          {section === "monitoring" && <MonitoringPage filter={filterBuilding} setFilter={setFilterBuilding} />}
          {section === "predictions" && <PredictionsPage />}
          {section === "anomalies" && <AnomaliesPage reviewed={reviewed} setReviewed={setReviewed} />}
          {section === "whatif" && <WhatIfPage scenario={scenario} setScenario={setScenario} baseline={baseline} simulatedEnergy={simulatedEnergy} simulatedCost={simulatedCost} simulatedCo2={simulatedCo2} energySaved={energySaved} scenarios={scenarios} saveScenario={saveScenario} />}
          {section === "solar" && <SolarPage scenario={scenario} setScenario={setScenario} baseline={baseline} settings={settings} solarContribution={solarContribution} />}
          {section === "peak" && <PeakPage />}
          {section === "recommendations" && <RecommendationsPage go={go} />}
          {section === "action" && <ActionCenterPage go={go} />}
          {section === "assistant" && <AssistantPage messages={assistantMessages} input={assistantInput} setInput={setAssistantInput} ask={askAssistant} />}
          {section === "impact" && <ImpactPage baseline={baseline} simulatedEnergy={simulatedEnergy} simulatedCost={simulatedCost} simulatedCo2={simulatedCo2} settings={settings} solarContribution={solarContribution} />}
          {section === "reports" && <ReportsPage downloadReport={downloadReport} go={go} />}
          {section === "sources" && <DataSourcesPage activeSource={activeSource} switchSource={switchSource} csvResult={csvResult} csvProgress={csvProgress} handleCsv={handleCsv} downloadTemplate={downloadTemplate} meterRunning={meterRunning} toggleMeter={toggleMeter} meterReading={meterReading} apiConfig={apiConfig} setApiConfig={setApiConfig} readOnly={isReadOnly} onSaveApiConfig={() => saveApiConfigMutation.mutate(apiConfig, { onSuccess: () => toast.success("API configuration saved", { description: "Configuration is persisted for future integration; no live data connection was claimed." }), onError: () => toast.error("API configuration could not be saved") })} />}
          {section === "data" && <DataPage csvResult={csvResult} csvProgress={csvProgress} handleCsv={handleCsv} activeSource={activeSource} switchSource={switchSource} meterRunning={meterRunning} toggleMeter={toggleMeter} meterReading={meterReading} />}
          {section === "users" && <UsersPage role={role} />}
          {section === "responsible" && <ResponsiblePage />}
          {section === "about" && <AboutPage go={go} />}
          {section === "settings" && <SettingsPage settings={settings} setSettings={setSettings} readOnly={isReadOnly} />}
        </div>
      </main>
    </div>
  </>;
}

function AuthPage({ view, setView, onAuth }: { view: "login" | "signup"; setView: (view: "login" | "signup") => void; onAuth: (profile: Profile) => void }) {
  const [showPassword, setShowPassword] = useState(false); const [error, setError] = useState("");
  const [form, setForm] = useState({ name: "", email: "", password: "", confirm: "", institution: "", role: "Facility Manager" as Role, terms: false });
  const submit = (event: React.FormEvent) => { event.preventDefault(); setError(""); if (view === "signup" && (!form.name || !form.institution || !form.terms)) return setError("Name, institution, and agreement are required."); if (!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(form.email)) return setError("Enter a valid email address."); if (form.password.length < 6) return setError("Password must be at least 6 characters."); if (view === "signup" && form.password !== form.confirm) return setError("Passwords do not match."); onAuth({ name: view === "login" ? (form.email ? form.email.split("@")[0] : "Facility Manager") : form.name, email: form.email || "demo@campusenergy.twin", institution: form.institution || "Northbridge Institute", role: view === "login" ? "Facility Manager" : form.role }); };
  return <div className="auth-shell"><div className="auth-visual"><div className="brand"><div className="brand-mark"><Zap size={18} fill="currentColor" /></div><div><strong>CampusEnergy</strong><span>Twin / 01</span></div></div><div className="auth-visual-copy"><div className="eyebrow"><span className="eyebrow-dot" /> SDG 7 · Affordable and Clean Energy</div><h1>Turn campus signals into <em>better decisions.</em></h1><p>AI-powered digital twin for sustainable campus energy management.</p><div className="auth-signal"><span>DATA SOURCES</span><b>→</b><span>AI ENGINE</span><b>→</b><span>DECISION SUPPORT</span></div></div></div><div className="auth-panel"><div className="auth-panel-inner"><span className="card-kicker">Campus workspace</span><h1>{view === "login" ? "Welcome back" : "Create your account"}</h1><p>{view === "login" ? "Sign in to continue to your energy intelligence workspace." : "Set up a prototype workspace profile."}</p><form onSubmit={submit}>{view === "signup" && <><label>Full Name<input value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} placeholder="B. Chandhra Shekhar" /></label><label>Institution / Organization<input value={form.institution} onChange={e => setForm({ ...form, institution: e.target.value })} placeholder="Northbridge Institute" /></label><label>Role<select value={form.role} onChange={e => setForm({ ...form, role: e.target.value as Role })}><option>Facility Manager</option><option>Sustainability Manager</option><option>Energy Analyst</option><option>Administrator</option></select></label></>}<label>Email<input type="email" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} placeholder="name@institution.edu" /></label><label>Password<div className="password-field"><input type={showPassword ? "text" : "password"} value={form.password} onChange={e => setForm({ ...form, password: e.target.value })} placeholder="At least 6 characters" /><button type="button" onClick={() => setShowPassword(!showPassword)}>{showPassword ? <EyeOff size={15} /> : <Eye size={15} />}</button></div></label>{view === "signup" && <><label>Confirm Password<input type="password" value={form.confirm} onChange={e => setForm({ ...form, confirm: e.target.value })} /></label><label className="check-label"><input type="checkbox" checked={form.terms} onChange={e => setForm({ ...form, terms: e.target.checked })} /> I agree to the platform's data and privacy terms.</label></>}{error && <div className="auth-error">{error}</div>}<button className="button button-primary auth-submit" type="submit">{view === "login" ? "Login" : "Create Account"} <ChevronRight size={16} /></button></form>{view === "login" ? <><button className="demo-login" onClick={() => onAuth({ name: "Demo / Evaluator", email: "demo@campusenergy.twin", institution: "Northbridge Institute", role: "Demo / Evaluator" })}>Continue with Demo Data <span>DEMO / SYNTHETIC DATA</span></button><div className="auth-switch">Don't have an account? <button onClick={() => setView("signup")}>Sign Up</button></div></> : <div className="auth-switch">Already have an account? <button onClick={() => setView("login")}>Login</button></div>}</div></div></div>;
}

function DataSourcesPage({ activeSource, switchSource, csvResult, csvProgress = 0, handleCsv, downloadTemplate, meterRunning, toggleMeter, meterReading, apiConfig, setApiConfig, readOnly, onSaveApiConfig }: { activeSource: DataSource; switchSource: (source: DataSource) => void; csvResult: { name: string; rows: number; buildings: number; missing: number; invalid: number; duplicates: number; valid: boolean; dateRange: string; totalEnergy: number; errors: { row: number; message: string; raw: string }[] } | null; csvProgress?: number; handleCsv: (event: ChangeEvent<HTMLInputElement>) => void; downloadTemplate: () => void; meterRunning: boolean; toggleMeter: () => void; meterReading: { building: string; meter: string; energy: number; power: number; voltage: number; current: number; timestamp: string }; apiConfig: ApiConfig; setApiConfig: (config: ApiConfig) => void; readOnly: boolean; onSaveApiConfig: () => void }) {
  return <><SectionTitle eyebrow="Data architecture · source center" title="Choose where the twin gets its signals." copy="One common data layer routes validated inputs into the dashboard, AI engine, digital twin, and decision-support workflows." action={<StatusPill tone={activeSource === "demo" ? "amber" : "teal"}>{sourceLabels[activeSource]}</StatusPill>} /><div className="source-flow"><span>DATA SOURCES</span><ChevronRight size={14} /><span>VALIDATION</span><ChevronRight size={14} /><span>DATABASE</span><ChevronRight size={14} /><span>AI ENGINE</span><ChevronRight size={14} /><span>DIGITAL TWIN</span></div><div className="source-grid"><Card className={activeSource === "uploaded" ? "source-card active" : "source-card"}><div className="source-level">LEVEL 1</div><h3>Manual / CSV data</h3><p>Upload historical or manually collected campus energy data. Invalid records are surfaced before import.</p><StatusPill tone="teal">Available</StatusPill><div className="source-actions"><label className="button button-primary upload-button"><Upload size={15} /> Upload CSV<input type="file" accept=".csv,text/csv" onChange={handleCsv} /></label><button className="button button-secondary" onClick={downloadTemplate}>Download template</button><button className="small-link" onClick={() => switchSource("uploaded")} disabled={!csvResult?.valid}>Import valid data <ChevronRight size={14} /></button></div>{csvProgress > 0 && <div className="csv-progress"><div className="csv-progress-top"><span>{csvProgress < 100 ? "Reading and validating CSV…" : "Validation complete"}</span><strong>{csvProgress}%</strong></div><div className="csv-progress-track"><span style={{ width: `${csvProgress}%` }} /></div><small>Upload stays in your browser until you explicitly import validated records.</small></div>}{csvResult && <><div className="source-metric"><strong>{num(csvResult.rows)} records · {csvResult.buildings} buildings</strong><span>{csvResult.dateRange} · {csvResult.invalid} invalid values · {csvResult.duplicates} duplicate rows</span><span>{csvResult.valid ? "Validation completed — ready for import." : "Validation needs attention before import."}</span></div>{csvResult.errors.length > 0 && <div className="csv-errors"><strong>Row diagnostics · showing up to 50 issues</strong>{csvResult.errors.slice(0, 4).map((error) => <div key={`${error.row}-${error.message}`}><b>Row {error.row}</b><span>{error.message}</span></div>)}{csvResult.errors.length > 4 && <small>+ {csvResult.errors.length - 4} more issue(s) shown in Data Management.</small>}</div>}</>}</Card><Card className={activeSource === "demo" ? "source-card active" : "source-card"}><div className="source-level">LEVEL 2</div><h3>Demo / synthetic data</h3><p>Use generated synthetic data to demonstrate the complete CampusEnergy Twin workflow.</p><StatusPill tone="amber">Synthetic / Demo data</StatusPill><div className="source-actions"><button className="button button-primary" onClick={() => switchSource("demo")}>Activate demo dataset</button><button className="button button-secondary" onClick={() => toast.success("Demo dataset regenerated", { description: "Realistic hourly building patterns remain synthetic and clearly labeled." })}>Generate new demo data</button><button className="small-link" onClick={() => { switchSource("demo"); toast.success("Demo data reset"); }}>Reset demo data <RefreshCw size={14} /></button></div></Card><Card className={activeSource === "simulated" ? "source-card active" : "source-card"}><div className="source-level">LEVEL 3</div><h3>Smart meter / IoT / API</h3><p>Connect future smart meters, IoT gateways, building-management systems, or external APIs without claiming a physical connection.</p><StatusPill tone={meterRunning ? "teal" : "blue"}>{meterRunning ? "SIMULATED REAL-TIME STREAM" : "Integration ready"}</StatusPill><div className="source-actions"><button className="button button-primary" onClick={toggleMeter}>{meterRunning ? "Stop stream" : "Start smart meter simulation"}</button><button className="button button-secondary" onClick={() => toast.info("Integration configuration ready", { description: "External API connection required. API keys are not stored in this frontend prototype." })}>Configure connection</button><button className="small-link" onClick={() => toast.info("Incoming data", { description: "JSON / CSV / API payload architecture is ready for a secure backend." })}>View incoming data <ChevronRight size={14} /></button></div>{meterRunning && <div className="meter-stream"><strong>SIMULATED SMART METER</strong><span>LIVE SIMULATED DATA · {meterReading.timestamp}</span><div><b>{meterReading.building}</b><span>{meterReading.meter}</span><strong>{meterReading.energy} kWh</strong><span>{meterReading.power} kW · {meterReading.voltage} V · {meterReading.current} A</span></div></div>}</Card></div><Card className="api-config-card"><div className="card-heading"><div><span className="card-kicker">Future integration</span><h3>API Connection</h3></div><StatusPill tone="blue">Connection required</StatusPill></div><div className="api-config-grid"><label className="form-field"><span>API Endpoint</span><input value={apiConfig.endpoint} onChange={(e) => setApiConfig({ ...apiConfig, endpoint: e.target.value })} placeholder="https://api.example.edu/meters" disabled={readOnly} /></label><label className="form-field"><span>API Key</span><input type="password" value={apiConfig.key} onChange={(e) => setApiConfig({ ...apiConfig, key: e.target.value })} placeholder="Not stored in this prototype" disabled={readOnly} /></label><label className="form-field"><span>Building</span><input value={apiConfig.building} onChange={(e) => setApiConfig({ ...apiConfig, building: e.target.value })} placeholder="Computer Labs" disabled={readOnly} /></label><label className="form-field"><span>Meter ID</span><input value={apiConfig.meter} onChange={(e) => setApiConfig({ ...apiConfig, meter: e.target.value })} placeholder="METER-001" disabled={readOnly} /></label><label className="form-field"><span>Update Interval</span><div className="input-with-suffix"><input type="number" min={5} value={apiConfig.interval} onChange={(e) => setApiConfig({ ...apiConfig, interval: Number(e.target.value) || 60 })} disabled={readOnly} /><span>seconds</span></div></label></div><div className="api-config-foot"><span><Info size={13} /> External API integration ready — connection required.</span><button className="button button-secondary" disabled={readOnly} onClick={onSaveApiConfig}>Save configuration</button></div></Card><Card className="source-history"><div className="card-heading"><div><span className="card-kicker">Data source history</span><h3>Recent workspace sources</h3></div><Database size={18} className="muted-icon" /></div><div className="history-row"><span>18 Sep 2026</span><strong>Demo Dataset</strong><span>6 buildings · 4,320 records</span><StatusPill tone={activeSource === "demo" ? "teal" : "slate"}>{activeSource === "demo" ? "Active" : "Available"}</StatusPill></div><div className="history-row"><span>17 Sep 2026</span><strong>Campus CSV</strong><span>{csvResult ? `${csvResult.buildings} buildings · ${csvResult.rows} records` : "Awaiting upload"}</span><StatusPill tone={activeSource === "uploaded" ? "teal" : "slate"}>{activeSource === "uploaded" ? "Active" : "Available"}</StatusPill></div><div className="history-row"><span>Prototype</span><strong>Smart Meter Simulation</strong><span>Generated locally · no physical meter</span><StatusPill tone={activeSource === "simulated" ? "teal" : "slate"}>{activeSource === "simulated" ? "Active" : "Ready"}</StatusPill></div></Card></>;
}

function UsersPage({ role }: { role: Role }) { const [users, setUsers] = useState([{ name: "Demo / Evaluator", email: "demo@campusenergy.twin", role: "Demo / Evaluator", status: "Active" }, { name: "Facility Manager", email: "facility@northbridge.edu", role: "Facility Manager", status: "Active" }]); return <><SectionTitle eyebrow="Administration · user management" title="Manage workspace access." copy="Administrator-only prototype controls for roles and account status. No passwords are displayed." action={<StatusPill tone={role === "Administrator" ? "teal" : "amber"}>{role === "Administrator" ? "Admin access" : "View only"}</StatusPill>} /><Card className="users-card"><div className="card-heading"><div><span className="card-kicker">User list</span><h3>{users.length} workspace profiles</h3></div><button className="button button-primary" disabled={role !== "Administrator"} onClick={() => setUsers([...users, { name: "New workspace user", email: "pending@campusenergy.twin", role: "Energy Analyst", status: "Invited" }])}><Plus size={15} /> Add user</button></div><div className="user-table">{users.map((user) => <div className="user-row" key={user.email}><div className="avatar">{user.name.slice(0,2).toUpperCase()}</div><div><strong>{user.name}</strong><span>{user.email}</span></div><span>{user.role}</span><StatusPill tone={user.status === "Active" ? "teal" : "amber"}>{user.status}</StatusPill><button className="small-link" disabled={role !== "Administrator"} onClick={() => setUsers(users.filter((item) => item.email !== user.email))}>Deactivate</button></div>)}</div></Card></>; }

function LandingPage({ go }: { go: (section: Section) => void }) {
  return <div className="landing-page"><div className="landing-grid"><div className="landing-copy"><div className="eyebrow"><span className="eyebrow-dot" /> Sustainability decision support · SDG 7</div><h1>See the <em>energy story</em> behind your campus.</h1><p className="landing-lede">CampusEnergy Twin turns campus energy patterns into a visual, explainable decision space — from measured data or a clearly labeled demo dataset.</p><div className="hero-actions"><button className="button button-primary" onClick={() => go("dashboard")}>Open dashboard <ChevronRight size={17} /></button><button className="button button-ghost" onClick={() => go("twin")}>Explore the digital twin <Grid2X2 size={17} /></button></div><div className="hero-proof"><div><strong>Monitor</strong><span>Energy patterns</span></div><div><strong>Predict</strong><span>Future demand</span></div><div><strong>Decide</strong><span>With context</span></div></div></div><div className="landing-visual"><div className="orb orb-one" /><div className="orb orb-two" /><div className="visual-topline"><span>Campus signal map</span><StatusPill tone="teal">Live demo</StatusPill></div><div className="campus-radar"><div className="radar-ring ring-one" /><div className="radar-ring ring-two" /><div className="radar-line" /><div className="radar-center"><Zap size={20} /><span>campus</span></div>{[["Academic", 28, 28, "teal"], ["Labs", 62, 18, "amber"], ["Hostel", 72, 64, "teal"], ["Library", 20, 67, "blue"]].map(([label, left, top, tone]) => <div key={String(label)} className={`radar-node node-${tone}`} style={{ left: `${left}%`, top: `${top}%` }}><span /><b>{label}</b></div>)}</div><div className="visual-footer"><div><span>Modeled campus load</span><strong>5,810 <small>kWh/day</small></strong></div><div className="signal-chart"><span /><span /><span /><span /><span /><span /><span /></div></div></div></div><div className="landing-flow"><div><span className="flow-number">01</span><strong>Campus</strong><small>rooms + facilities</small></div><GitBranch size={17} /><div><span className="flow-number">02</span><strong>Energy data</strong><small>measured or demo</small></div><GitBranch size={17} /><div><span className="flow-number">03</span><strong>AI + twin</strong><small>patterns, not guesses</small></div><GitBranch size={17} /><div><span className="flow-number">04</span><strong>Scenarios</strong><small>test before acting</small></div></div><div className="landing-why"><div><div className="eyebrow">Why this exists</div><h2>Energy decisions need a campus context.</h2><p>Educational institutions distribute energy demand across classrooms, labs, hostels, libraries and shared spaces. The twin brings those signals into one calm workspace — so teams can investigate before they intervene.</p></div><Card className="principle-card"><div className="principle-icon"><Leaf size={20} /></div><span>Core philosophy</span><strong>Monitor → Predict → Detect → Simulate → Recommend → Act</strong><p>Every output is labeled as data-derived, predicted, estimated, simulated, or assumption-based.</p></Card></div></div>;
}

function DashboardPage({ go, role, sourceLabel, baseline }: { go: (section: Section) => void; role: Role; sourceLabel: string; baseline: number }) {
  const sourceDescription = sourceLabel.startsWith("UPLOADED") ? "Uploaded campus records feed this shared dashboard and analytics view." : sourceLabel.startsWith("SIMULATED") ? "Generated meter readings feed this shared prototype source state." : "Results are illustrative and must not be interpreted as real campus measurements.";
  return <><SectionTitle eyebrow="DEMO PERIOD • SEPTEMBER 2026" title={`Good morning, ${role}.`} copy="A calm view of what your campus energy is doing — and where to look next." action={<button className="button button-secondary" onClick={() => go("reports")}><Download size={16} /> Export report</button>} /><div className="notice-strip"><div className="notice-icon"><Info size={15} /></div><div><strong>{sourceLabel}</strong><span>{sourceDescription}</span></div><button onClick={() => go("data")}>Load your data <ChevronRight size={14} /></button></div><div className="kpi-grid"><KpiCard label="Today's modeled consumption" value="5,810 kWh" detail="modeled campus total" icon={Zap} accent="accent-teal" trend="4.8%" /><KpiCard label="Monthly modeled consumption" value="168.4 MWh" detail="estimated from demo series" icon={BarChart3} accent="accent-blue" trend="2.1%" positive={false} /><KpiCard label="Predicted peak" value="960 kW" detail="high window · 14:00–16:00" icon={Gauge} accent="accent-amber" trend="6.2%" positive={false} /><KpiCard label="Estimated daily cost" value="₹523" detail="at configured ₹9/kWh" icon={BatteryCharging} accent="accent-violet" /><KpiCard label="Estimated CO₂e" value="3.08 t" detail="factor · 0.53 kg/kWh" icon={CloudSun} accent="accent-slate" /><KpiCard label="Renewable contribution" value="0%" detail="no solar in current baseline" icon={SunMedium} accent="accent-sun" /> </div><div className="dashboard-grid"><Card className="chart-card chart-wide"><div className="card-heading"><div><span className="card-kicker">Trendline</span><h3>Campus energy consumption</h3></div><div className="chart-legend"><span><i className="legend-swatch teal" /> Actual</span><span><i className="legend-swatch blue" /> Model prediction</span><select defaultValue="7 days"><option>7 days</option><option>30 days</option><option>90 days</option></select></div></div><div className="chart-wrap"><ResponsiveContainer width="100%" height="100%"><ComposedChart data={trendData} margin={{ top: 12, right: 8, left: -18, bottom: 0 }}><defs><linearGradient id="tealFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#13b8a6" stopOpacity={0.24} /><stop offset="100%" stopColor="#13b8a6" stopOpacity={0} /></linearGradient></defs><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="#e8eeeb" /><XAxis dataKey="label" axisLine={false} tickLine={false} tick={{ fill: "#87948e", fontSize: 12 }} /><YAxis axisLine={false} tickLine={false} tick={{ fill: "#87948e", fontSize: 12 }} /><Tooltip content={<ChartTooltip />} /><Area type="monotone" dataKey="actual" name="Actual" stroke="#13b8a6" strokeWidth={2.5} fill="url(#tealFill)" /><Line type="monotone" dataKey="prediction" name="Prediction" stroke="#7698d6" strokeWidth={2} strokeDasharray="5 4" dot={false} /></ComposedChart></ResponsiveContainer></div><div className="chart-footnote"><span><Info size={13} /> Daily totals are generated from the active {sourceLabel.toLowerCase()} source.</span><button onClick={() => go("predictions")}>Open prediction detail <ChevronRight size={14} /></button></div></Card><Card className="building-card"><div className="card-heading"><div><span className="card-kicker">Where energy is going</span><h3>Building consumption</h3></div><button className="small-link" onClick={() => go("twin")}>View twin <ChevronRight size={14} /></button></div><div className="building-bars">{buildings.slice(0, 5).map((building) => <div className="building-bar-row" key={building.name}><div className="bar-name"><span className={`building-dot ${building.tone}`} />{building.name}</div><div className="bar-track"><span style={{ width: `${building.share * 2.6}%`, background: building.tone === "amber" ? "#e5a93d" : "#13b8a6" }} /></div><strong>{num(building.energy)}</strong></div>)}</div><div className="building-card-footer"><span>Share of modeled campus total</span><strong>{num(baseline)} kWh/day</strong></div></Card></div><div className="dashboard-lower"><Card className="insight-card"><div className="insight-top"><div className="ai-orb"><Sparkles size={18} /></div><div><span className="card-kicker">AI insight · explainable</span><h3>Afternoon lab demand is worth a closer look.</h3></div><StatusPill tone="amber">Attention</StatusPill></div><p>Computer Labs shows higher-than-baseline consumption during afternoon operating hours. Possible contributors include cooling demand, extended equipment runtime, or occupancy patterns.</p><div className="insight-evidence"><span><Target size={14} /> Evidence</span><strong>1,850 kWh/day · 26.7% above baseline</strong></div><button className="button button-dark" onClick={() => go("recommendations")}>Review recommendation <ChevronRight size={15} /></button></Card><Card className="peak-card"><div className="card-heading"><div><span className="card-kicker">Demand watch</span><h3>Peak window</h3></div><Gauge size={19} className="muted-icon" /></div><div className="peak-number">960 <small>kW</small></div><div className="peak-window"><span className="peak-line" />Predicted high-demand window <strong>14:00–16:00</strong></div><p>Consider shifting flexible, non-essential activities away from this period. The system does not force schedules.</p><button className="small-link" onClick={() => go("peak")}>See peak analysis <ChevronRight size={14} /></button></Card><Card className="status-card"><div className="card-heading"><div><span className="card-kicker">Campus status</span><h3>At a glance</h3></div><StatusPill>Stable</StatusPill></div><div className="status-rows"><div><span>Buildings monitored</span><strong>06 / 06</strong></div><div><span>Patterns for review</span><strong className="amber-text">02</strong></div><div><span>Data completeness</span><strong>96%</strong></div><div><span>Scenario savings</span><strong className="teal-text">Estimate only</strong></div></div></Card></div></>;
}

function TwinPage({ selected, onSelect }: { selected: typeof buildings[number]; onSelect: (building: typeof buildings[number]) => void }) {
  return <><SectionTitle eyebrow="Digital representation · campus view" title="Explore the campus twin." copy="A conceptual map of energy behavior across buildings. It is not a real electrical grid topology." action={<div className="segmented"><button className="selected">Campus view</button><button>Energy flow</button></div>} /><div className="twin-layout"><Card className="twin-map-card"><div className="map-toolbar"><div><span className="card-kicker">Northbridge Institute</span><h3>Campus signal map</h3></div><div className="map-legend"><span><i className="legend-dot teal" /> Normal</span><span><i className="legend-dot amber" /> Attention</span></div></div><div className="campus-map"><div className="map-road road-a" /><div className="map-road road-b" /><div className="map-park park-a" /><div className="map-park park-b" />{buildings.map((building, index) => <button key={building.name} className={`map-building map-building-${index} ${selected.name === building.name ? "map-selected" : ""}`} onClick={() => onSelect(building)}><div className={`map-building-icon ${building.tone}`}><Building2 size={18} /></div><div><strong>{building.name}</strong><span>{num(building.energy)} kWh/day</span></div><span className={`map-status ${building.tone}`} /> </button>)}<div className="map-compass"><span>N</span><div>↑</div></div><div className="map-label">Conceptual campus layer</div></div><div className="map-foot"><span><Info size={13} /> Status combines modeled energy, baseline deviation, and data completeness.</span><span>Last demo refresh · 09:42</span></div></Card><Card className="twin-detail"><div className="detail-header"><div className={`detail-avatar ${selected.tone}`}>{selected.short}</div><div><span className="card-kicker">Selected building</span><h3>{selected.name}</h3><StatusPill tone={selected.tone === "amber" ? "amber" : selected.tone === "slate" ? "slate" : "teal"}>{selected.status}</StatusPill></div></div><div className="detail-primary"><span>Modeled daily consumption</span><strong>{num(selected.energy)} <small>kWh</small></strong><div className="detail-trend"><Sparkles size={13} /> Predicted tomorrow {num(selected.predicted)} kWh</div></div><div className="detail-metrics"><div><Users size={15} /><span>Occupancy</span><strong>{selected.occupancy}%</strong></div><div><CalendarDays size={15} /><span>Operating hours</span><strong>{selected.hours}</strong></div></div><div className="profile-title"><span>Load profile</span><small>modeled share</small></div><div className="profile-bars"><div><span>AC usage</span><MiniBar value={selected.ac} color="#7698d6" /><strong>{selected.ac}%</strong></div><div><span>Lighting</span><MiniBar value={selected.lighting} color="#e5a93d" /><strong>{selected.lighting}%</strong></div><div><span>Equipment</span><MiniBar value={selected.equipment} color="#13b8a6" /><strong>{selected.equipment}%</strong></div></div><div className="detail-trend-head"><span>Recent trend</span><StatusPill tone={selected.energy > selected.baseline ? "amber" : "teal"}>{selected.energy > selected.baseline ? "Above baseline" : "Within baseline"}</StatusPill></div><Spark data={selected.trend} color={selected.tone === "amber" ? "#e5a93d" : "#13b8a6"} /><div className="detail-note"><Sparkles size={15} /><span><strong>AI note:</strong> {selected.name === "Computer Labs" ? "Afternoon demand is higher than the modeled baseline. Review cooling and equipment schedules before inferring a physical cause." : "This building is tracking close to its modeled operating profile in the current demo dataset."}</span></div></Card></div><div className="flow-strip"><div className="flow-step"><div className="flow-icon"><Zap size={17} /></div><span>Grid / renewable source</span></div><ChevronRight size={16} /><div className="flow-step"><div className="flow-icon"><Building2 size={17} /></div><span>Campus distribution</span></div><ChevronRight size={16} /><div className="flow-step"><div className="flow-icon"><PanelsTopLeft size={17} /></div><span>Buildings & rooms</span></div><ChevronRight size={16} /><div className="flow-step"><div className="flow-icon"><Activity size={17} /></div><span>Modeled consumption</span></div></div></>;
}

function MonitoringPage({ filter, setFilter }: { filter: string; setFilter: (value: string) => void }) {
  const selected = filter === "All buildings" ? buildings[0] : buildings.find((building) => building.name === filter) || buildings[0];
  return <><SectionTitle eyebrow="Building analytics" title="Monitor the energy picture." copy="Choose a building and compare its modeled profile with the campus baseline." action={<button className="button button-secondary"><Filter size={16} /> Advanced filters</button>} /><div className="filter-row"><div className="filter-field"><Building2 size={15} /><select value={filter} onChange={(e) => setFilter(e.target.value)}><option>All buildings</option>{buildings.map((building) => <option key={building.name}>{building.name}</option>)}</select></div><div className="filter-field"><CalendarDays size={15} /><select defaultValue="Last 7 days"><option>Last 7 days</option><option>Last 30 days</option><option>Custom period</option></select></div><div className="filter-field"><Activity size={15} /><select defaultValue="Daily"><option>Hourly</option><option>Daily</option><option>Monthly</option></select></div><div className="filter-meta"><span className="live-dot" /> Baseline synced · demo</div></div><div className="monitor-grid"><Card className="chart-card chart-large"><div className="card-heading"><div><span className="card-kicker">Consumption trend</span><h3>{selected.name} · daily modeled consumption</h3></div><StatusPill tone={selected.tone === "amber" ? "amber" : "teal"}>{selected.status}</StatusPill></div><div className="chart-wrap chart-wrap-large"><ResponsiveContainer width="100%" height="100%"><AreaChart data={trendData}><defs><linearGradient id="monitorFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#7698d6" stopOpacity={0.28} /><stop offset="100%" stopColor="#7698d6" stopOpacity={0} /></linearGradient></defs><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="#e8eeeb" /><XAxis dataKey="label" axisLine={false} tickLine={false} tick={{ fill: "#87948e", fontSize: 12 }} /><YAxis axisLine={false} tickLine={false} tick={{ fill: "#87948e", fontSize: 12 }} /><Tooltip content={<ChartTooltip />} /><Area type="monotone" dataKey="actual" name="Observed / modeled" stroke="#7698d6" strokeWidth={2.5} fill="url(#monitorFill)" /></AreaChart></ResponsiveContainer></div><div className="compare-strip"><div><span>Average / day</span><strong>{num(selected.energy)} kWh</strong><small>selected building</small></div><div><span>Campus average</span><strong>968 kWh</strong><small>6 building mean</small></div><div><span>Period change</span><strong className="amber-text">+6.4%</strong><small>vs previous period</small></div></div></Card><Card className="metric-stack"><div className="card-heading"><div><span className="card-kicker">Selected profile</span><h3>Energy signals</h3></div></div><div className="metric-stack-row"><div className="metric-stack-icon blue"><Gauge size={16} /></div><div><span>Peak consumption</span><small>Highest modeled interval</small></div><strong>312 kW</strong></div><div className="metric-stack-row"><div className="metric-stack-icon teal"><Users size={16} /></div><div><span>Average occupancy</span><small>Aggregated, non-identifying</small></div><strong>{selected.occupancy}%</strong></div><div className="metric-stack-row"><div className="metric-stack-icon amber"><ClockIcon /></div><div><span>Operating hours</span><small>Configured schedule</small></div><strong>{selected.hours}</strong></div><div className="metric-stack-row"><div className="metric-stack-icon violet"><BatteryCharging size={16} /></div><div><span>Estimated cost</span><small>At configurable tariff</small></div><strong>₹112/day</strong></div><div className="metric-stack-row"><div className="metric-stack-icon slate"><CloudSun size={16} /></div><div><span>Estimated emissions</span><small>At configurable factor</small></div><strong>0.66 t</strong></div></Card></div><Card className="table-card"><div className="card-heading"><div><span className="card-kicker">Campus comparison</span><h3>Building vs campus average</h3></div><button className="small-link">Download CSV <Download size={14} /></button></div><div className="table-scroll"><table><thead><tr><th>Building</th><th>Modeled energy</th><th>Share</th><th>Occupancy</th><th>Baseline delta</th><th>Status</th></tr></thead><tbody>{buildings.map((building) => <tr key={building.name}><td><div className="table-building"><span className={`building-dot ${building.tone}`} />{building.name}</div></td><td>{num(building.energy)} kWh</td><td>{building.share}%</td><td>{building.occupancy}%</td><td className={building.energy > building.baseline ? "amber-text" : "teal-text"}>{building.energy > building.baseline ? "+" : "−"}{Math.abs(Math.round((building.energy - building.baseline) / building.baseline * 100))}%</td><td><StatusPill tone={building.tone === "amber" ? "amber" : building.tone === "slate" ? "slate" : "teal"}>{building.status}</StatusPill></td></tr>)}</tbody></table></div></Card></>;
}

function ClockIcon() { return <CalendarDays size={16} />; }

function PredictionsPage() {
  return <><SectionTitle eyebrow="AI / model prediction" title="Forecast the next demand curve." copy="A transparent local baseline model estimates future consumption from historical patterns, time, occupancy, and operating signals." action={<div className="title-actions"><button className="explain-link" onClick={() => toast.info("Why this prediction?", { description: "The model considers historical energy, time of day, day of week, aggregated occupancy, temperature where available, AC usage, and equipment usage." })}><Info size={14} /> Why did the prediction change?</button><StatusPill tone="blue">Model prediction</StatusPill></div>} /><div className="prediction-grid"><Card className="chart-card chart-large"><div className="card-heading"><div><span className="card-kicker">Six-day outlook</span><h3>Predicted vs observed consumption</h3></div><div className="chart-legend"><span><i className="legend-swatch teal" /> Observed</span><span><i className="legend-swatch blue" /> Predicted</span><span><i className="legend-swatch range" /> Uncertainty</span></div></div><div className="chart-wrap chart-wrap-large"><ResponsiveContainer width="100%" height="100%"><ComposedChart data={predictionData}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="#e8eeeb" /><XAxis dataKey="day" axisLine={false} tickLine={false} tick={{ fill: "#87948e", fontSize: 12 }} /><YAxis axisLine={false} tickLine={false} tick={{ fill: "#87948e", fontSize: 12 }} /><Tooltip content={<ChartTooltip />} /><Area dataKey="upper" name="Upper range" stroke="none" fill="#dcecf0" /><Area dataKey="lower" name="Lower range" stroke="none" fill="#fff" /><Line type="monotone" dataKey="model" name="Predicted" stroke="#7698d6" strokeWidth={2.5} dot={{ r: 3, fill: "#7698d6" }} /><Line type="monotone" dataKey="actual" name="Observed" stroke="#13b8a6" strokeWidth={2.5} dot={{ r: 3, fill: "#13b8a6" }} /></ComposedChart></ResponsiveContainer></div><div className="model-foot"><Info size={14} /> The shaded area is an uncertainty indication based on demo-series variability. It is not a confidence guarantee.</div></Card><Card className="model-card"><div className="model-score"><span>Model performance</span><strong>Good enough for a demo</strong><div className="score-meter"><span style={{ width: "78%" }} /></div><small>Illustrative holdout evaluation · MAE 184 kWh · R² 0.78</small></div><div className="model-divider" /><div><span className="card-kicker">Input features</span><div className="feature-list"><div><Check size={14} /> Historical consumption</div><div><Check size={14} /> Hour and day of week</div><div><Check size={14} /> Aggregated occupancy</div><div><Check size={14} /> AC, lighting, equipment usage</div><div><Check size={14} /> Temperature where available</div></div></div><div className="explain-note"><Sparkles size={15} /><span>Prediction is based primarily on historical consumption, time of day, occupancy, and operating patterns.</span></div></Card></div><div className="prediction-cards"><Card><span className="card-kicker">Predicted peak</span><div className="big-metric">960 <small>kW</small></div><p>Likely window tomorrow: <strong>14:00–16:00</strong></p></Card><Card><span className="card-kicker">Uncertainty range</span><div className="big-metric">±380 <small>kWh</small></div><p>Wider when fewer recent observations are available.</p></Card><Card><span className="card-kicker">Training source</span><div className="big-metric">42 <small>days</small></div><p>Demo time-series records across six campus areas.</p></Card></div></>;
}

function AnomaliesPage({ reviewed, setReviewed }: { reviewed: string[]; setReviewed: (ids: string[]) => void }) {
  return <><SectionTitle eyebrow="AI / pattern detection" title="Investigate unusual usage." copy="Patterns are flagged when they deviate from a building and time-period baseline. The system suggests possible contributors; it does not claim exact causes." action={<div className="anomaly-summary"><strong>{anomalies.length - reviewed.length}</strong><span>open reviews</span></div>} /><div className="anomaly-banner"><div className="anomaly-banner-icon"><AlertTriangle size={18} /></div><div><strong>Review before acting</strong><span>Unusual consumption can reflect occupancy, extended hours, cooling demand, equipment use, or data quality.</span></div><button className="explain-link" onClick={() => toast.info("AI Explanation", { description: "The value is above the modeled baseline for this building and time period. Possible contributing factors are not confirmed causes." })}><Info size={14} /> AI explanation</button><StatusPill tone="amber">Human review</StatusPill></div><div className="anomaly-list">{anomalies.map((anomaly) => { const done = reviewed.includes(anomaly.id); return <Card className={`anomaly-card ${done ? "reviewed" : ""}`} key={anomaly.id}><div className={`severity ${anomaly.severity.toLowerCase()}`}><AlertTriangle size={17} /><span>{anomaly.severity}</span></div><div className="anomaly-main"><div className="anomaly-title"><div><span className="card-kicker">{anomaly.building} · {anomaly.place}</span><h3>{done ? "Marked as reviewed" : "Unusual consumption detected"}</h3></div><span className="anomaly-when">{anomaly.when}</span></div><div className="anomaly-values"><div><span>Observed</span><strong>{anomaly.observed} <small>kWh</small></strong></div><div><span>Expected range</span><strong>{anomaly.baseline} <small>kWh</small></strong></div><div><span>Deviation</span><strong className={anomaly.deviation.startsWith("−") ? "teal-text" : "amber-text"}>{anomaly.deviation}</strong></div></div><div className="possible-factor"><Info size={14} /><span><strong>Possible contributing factors:</strong> {anomaly.factor}. Exact physical cause is unknown.</span></div></div><button className={`button ${done ? "button-secondary" : "button-dark"}`} onClick={() => setReviewed(done ? reviewed.filter((id) => id !== anomaly.id) : [...reviewed, anomaly.id])}>{done ? <><Check size={15} /> Reviewed</> : "Mark as reviewed"}</button></Card>; })}</div><div className="method-grid"><Card><span className="card-kicker">Detection method</span><h3>Baseline + deviation rules</h3><p>For the demo, a statistical baseline is combined with a threshold rule to surface values that are materially above or below the expected range for a building and time period.</p></Card><Card><span className="card-kicker">Explainability</span><h3>Why this was flagged</h3><p>Each alert shows the observed value, expected range, deviation, data context, and possible contributors without revealing hidden chain-of-thought or asserting an equipment fault.</p></Card></div></>;
}

function SliderField({ label, value, min, max, suffix = "%", onChange, hint }: { label: string; value: number; min: number; max: number; suffix?: string; onChange: (value: number) => void; hint: string }) {
  return <div className="slider-field"><div className="slider-label"><span>{label}</span><strong>{value}{suffix}</strong></div><input type="range" min={min} max={max} value={value} onChange={(e) => onChange(Number(e.target.value))} /><small>{hint}</small></div>;
}

function WhatIfPage({ scenario, setScenario, baseline, simulatedEnergy, simulatedCost, simulatedCo2, energySaved, scenarios, saveScenario }: { scenario: ScenarioControls; setScenario: (value: ScenarioControls) => void; baseline: number; simulatedEnergy: number; simulatedCost: number; simulatedCo2: number; energySaved: number; scenarios: Scenario[]; saveScenario: () => void }) {
  return <><SectionTitle eyebrow="Scenario lab · decision support" title="Test an intervention before you act." copy="Change assumptions to estimate direction and magnitude. Scenario outputs are not guaranteed savings." action={<button className="button button-primary" onClick={saveScenario}><Plus size={16} /> Save scenario</button>} /><div className="simulator-layout"><Card className="assumption-card"><div className="card-heading"><div><span className="card-kicker">Control panel</span><h3>Scenario assumptions</h3></div><SlidersHorizontal size={18} className="muted-icon" /></div><SliderField label="Lighting converted to LED" value={scenario.led} min={0} max={100} onChange={(value) => setScenario({ ...scenario, led: value })} hint="Modeled lighting share adjusts with the upgrade percentage." /><SliderField label="AC usage reduction" value={scenario.ac} min={0} max={30} onChange={(value) => setScenario({ ...scenario, ac: value })} hint="A planning assumption for cooling runtime, not a control signal." /><SliderField label="Flexible schedule shift" value={scenario.schedule} min={0} max={30} onChange={(value) => setScenario({ ...scenario, schedule: value })} hint="Estimated load shifting away from the 14:00–16:00 window." /><SliderField label="Equipment shutdown" value={scenario.equipment} min={0} max={30} onChange={(value) => setScenario({ ...scenario, equipment: value })} hint="Assumed reduction in unnecessary equipment runtime." /><div className="solar-inline"><div><SunMedium size={17} /><span>Solar generation</span></div><div className="input-with-suffix"><input type="number" value={scenario.solar} min={0} max={5000} onChange={(e) => setScenario({ ...scenario, solar: Number(e.target.value) || 0 })} /><span>kWh/day</span></div></div><div className="assumption-note"><Info size={14} /> All calculations use the configurable tariff and emissions factor in Settings.</div></Card><Card className="simulation-output"><div className="output-top"><div><span className="card-kicker">Simulation Result — Estimated</span><h3>Current vs simulated campus</h3></div><div className="title-actions"><button className="explain-link" onClick={() => toast.info("Simulation explanation", { description: "The result applies visible LED, AC, schedule, equipment, and solar assumptions to the common campus baseline. Actual savings depend on real campus conditions, equipment performance, tariff, and implementation." })}><Info size={14} /> Why am I seeing this?</button><StatusPill tone="amber">Estimate only</StatusPill></div></div><div className="scenario-compare"><div className="scenario-column"><span>Current campus</span><strong>{num(baseline)} <small>kWh/day</small></strong><div className="scenario-line"><span style={{ width: "100%" }} /></div><small>Baseline / demo series</small></div><div className="scenario-arrow"><ArrowDownRight size={20} /><strong>−{num(energySaved)} kWh</strong></div><div className="scenario-column simulated"><span>Simulated scenario</span><strong>{num(simulatedEnergy)} <small>kWh/day</small></strong><div className="scenario-line"><span style={{ width: `${(simulatedEnergy / baseline) * 100}%` }} /></div><small>Estimated after assumptions</small></div></div><div className="output-metrics"><div><span>Estimated energy reduction</span><strong>{pct((energySaved / baseline) * 100)}</strong><small>{num(energySaved)} kWh/day</small></div><div><span>Estimated cost</span><strong>{money(simulatedCost)}</strong><small>vs {money(baseline * 9 / 100)} baseline</small></div><div><span>Estimated CO₂e</span><strong>{simulatedCo2.toFixed(2)} t</strong><small>factor-dependent estimate</small></div></div><div className="waterfall"><span>Lighting</span><i style={{ width: `${scenario.led}%` }} /><span>AC</span><i style={{ width: `${scenario.ac * 2}%` }} /><span>Schedule</span><i style={{ width: `${scenario.schedule * 2}%` }} /><span>Solar</span><i style={{ width: `${Math.min(100, scenario.solar / 20)}%` }} /></div><div className="output-disclaimer"><Sparkles size={15} /><span>This is a scenario estimate based on user-entered assumptions. Actual savings require implementation and measurement.</span></div></Card></div><Card className="table-card"><div className="card-heading"><div><span className="card-kicker">Compare saved scenarios</span><h3>Assumptions stay visible</h3></div><span className="table-caption">Simulation / Estimate</span></div><div className="table-scroll"><table><thead><tr><th>Scenario</th><th>Key assumptions</th><th>Energy</th><th>Cost</th><th>CO₂e</th></tr></thead><tbody>{scenarios.map((item) => <tr key={item.name}><td><strong>{item.name}</strong><small className="table-sub">{item.tag}</small></td><td><span className="assumption-tags">{item.led > 0 && `LED ${item.led}% `}{item.schedule > 0 && `Shift ${item.schedule}% `}{item.solar > 0 && `Solar ${num(item.solar)} kWh`}{!item.led && !item.schedule && !item.solar && "Current baseline"}</span></td><td>{num(item.energy)} kWh</td><td>{money(item.cost)}</td><td>{item.co2.toFixed(2)} t</td></tr>)}</tbody></table></div></Card></>;
}

function SolarPage({ scenario, setScenario, baseline, settings, solarContribution }: { scenario: ScenarioControls; setScenario: (value: ScenarioControls) => void; baseline: number; settings: { tariff: number; emissions: number }; solarContribution: number }) {
  const generation = scenario.solar || 1600;
  const grid = Math.max(0, baseline - generation);
  return <><SectionTitle eyebrow="Renewable planning" title="Size a solar scenario." copy="Explore how roof area and assumed generation could change modeled grid-energy requirements." action={<StatusPill tone="blue">Estimated / simulated</StatusPill>} /><div className="solar-grid"><Card className="solar-control"><div className="card-heading"><div><span className="card-kicker">Planning inputs</span><h3>Solar assumptions</h3></div><SunMedium size={19} className="sun-icon" /></div><div className="form-field"><label>Available roof area</label><div className="input-with-suffix"><input type="number" value={scenario.roof} onChange={(e) => setScenario({ ...scenario, roof: Number(e.target.value) || 0 })} /><span>m²</span></div><small>Assumption entered by the user.</small></div><div className="form-field"><label>Solar capacity</label><div className="input-with-suffix"><input type="number" value={Math.round(generation / 4)} onChange={(e) => setScenario({ ...scenario, solar: Number(e.target.value) * 4 || 0 })} /><span>kW</span></div><small>Generation is modeled at 4 kWh/day per kW for this demo.</small></div><div className="form-field"><label>Expected daily generation</label><div className="input-with-suffix"><input type="number" value={generation} onChange={(e) => setScenario({ ...scenario, solar: Number(e.target.value) || 0 })} /><span>kWh</span></div><small>Does not represent actual solar performance.</small></div><div className="solar-assumption"><ThermometerSun size={15} /><span>Transparent assumption: generation varies with roof orientation, weather, equipment, shading, maintenance, and grid connection.</span></div></Card><Card className="solar-output"><div className="solar-orbit"><div className="solar-sun"><SunMedium size={28} /></div><div className="orbit-ring" /><div className="orbit-label orbit-a"><strong>{num(generation)}</strong><span>solar kWh/day</span></div><div className="orbit-label orbit-b"><strong>{num(grid)}</strong><span>grid required</span></div></div><div className="renewable-share"><span>Modeled renewable contribution</span><strong>{pct(solarContribution)}</strong><div className="progress-line"><span style={{ width: `${solarContribution}%` }} /></div><small>Based on the current {num(baseline)} kWh/day baseline.</small></div><div className="solar-metrics"><div><span>Estimated cost difference</span><strong className="teal-text">−{money(generation * settings.tariff / 100)}</strong><small>before fixed charges</small></div><div><span>Estimated CO₂e difference</span><strong className="teal-text">−{(generation * settings.emissions / 1000).toFixed(2)} t</strong><small>factor-dependent</small></div></div></Card></div><Card className="solar-note"><Leaf size={17} /><div><strong>Potential contribution to SDG 7</strong><p>A solar scenario can help teams investigate affordable and cleaner energy pathways. It is a planning estimate — not a claim about installed capacity, real generation, or achieved targets.</p></div></Card></>;
}

function PeakPage() {
  return <><SectionTitle eyebrow="Peak demand analysis" title="Understand the campus peak." copy="Use the historical and predicted curves to identify when flexible activities could potentially move." action={<StatusPill tone="amber">Decision support</StatusPill>} /><div className="peak-grid"><Card className="chart-card chart-large"><div className="card-heading"><div><span className="card-kicker">Load curve</span><h3>Demand by time of day</h3></div><div className="chart-legend"><span><i className="legend-swatch amber" /> Modeled demand</span></div></div><div className="chart-wrap chart-wrap-large"><ResponsiveContainer width="100%" height="100%"><AreaChart data={hourlyData}><defs><linearGradient id="peakFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#e5a93d" stopOpacity={0.3} /><stop offset="100%" stopColor="#e5a93d" stopOpacity={0} /></linearGradient></defs><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="#e8eeeb" /><XAxis dataKey="time" axisLine={false} tickLine={false} tick={{ fill: "#87948e", fontSize: 12 }} tickFormatter={(value) => `${value}:00`} /><YAxis axisLine={false} tickLine={false} tick={{ fill: "#87948e", fontSize: 12 }} /><Tooltip content={<ChartTooltip />} /><Area type="monotone" dataKey="value" name="Demand" stroke="#e5a93d" strokeWidth={2.5} fill="url(#peakFill)" /></AreaChart></ResponsiveContainer></div><div className="peak-callout"><div className="peak-callout-number">14:00–16:00</div><div><strong>Predicted high-demand window</strong><span>Historical peak has also clustered in the afternoon in the demo series.</span></div></div></Card><div className="peak-side"><Card><div className="big-stat"><span>Historical peak</span><strong>960 <small>kW</small></strong><StatusPill tone="amber">Friday · 15:00</StatusPill></div></Card><Card><div className="big-stat"><span>Predicted peak</span><strong>980 <small>kW</small></strong><StatusPill tone="blue">Tomorrow · estimate</StatusPill></div></Card><Card className="recommendation-mini"><div className="mini-icon"><GitBranch size={17} /></div><strong>Consider shifting flexible activities</strong><p>Move non-essential activities away from the predicted peak period when operationally appropriate. The twin does not force a schedule.</p></Card></div></div><Card className="peak-table"><div className="card-heading"><div><span className="card-kicker">Flexible actions to investigate</span><h3>Questions for a facilities review</h3></div></div><div className="action-list"><div><span className="action-number">01</span><div><strong>Can non-essential lab runs shift?</strong><p>Compare equipment schedules with the predicted 14:00–16:00 window.</p></div><ChevronRight size={16} /></div><div><span className="action-number">02</span><div><strong>Are cooling setpoints consistent?</strong><p>Review comfort, occupancy, and cooling assumptions together.</p></div><ChevronRight size={16} /></div><div><span className="action-number">03</span><div><strong>Is peak data complete?</strong><p>Confirm that a high value is not a missing or duplicated record.</p></div><ChevronRight size={16} /></div></div></Card></>;
}

function RecommendationsPage({ go }: { go: (section: Section) => void }) {
  const recs = [
    { tone: "amber", title: "Review Computer Lab shutdown and cooling schedules", reason: "Repeated high modeled consumption during lower-occupancy afternoon periods.", evidence: "1,850 kWh/day · 26.7% above modeled baseline", impact: "Could reduce unnecessary runtime if validated", confidence: "Medium" },
    { tone: "blue", title: "Investigate flexible activity shifting", reason: "Predicted demand is higher between 14:00 and 16:00 tomorrow.", evidence: "Predicted peak · 980 kW", impact: "May flatten the peak window", confidence: "Medium" },
    { tone: "teal", title: "Test an LED upgrade scenario", reason: "Lighting represents a meaningful modeled share of campus demand.", evidence: "Lighting share · 28% across demo series", impact: "Scenario estimate available in the lab", confidence: "High" },
  ];
  return <><SectionTitle eyebrow="AI recommendation engine" title="From signals to next questions." copy="Recommendations translate analytics into practical review prompts. They do not automate decisions or fabricate savings." action={<button className="button button-secondary" onClick={() => go("whatif")}><Play size={15} /> Open scenario lab</button>} /><div className="recommendation-intro"><div className="ai-orb large"><Sparkles size={22} /></div><div><strong>Recommendations are decision support</strong><p>Every suggestion includes its reason, supporting data, expected direction of impact, assumptions, and confidence.</p></div><button className="explain-link" onClick={() => toast.info("Recommendation explanation", { description: "Recommendations are generated from the active demo dataset, model outputs, scenario assumptions, and stated limitations." })}><Info size={14} /> AI explanation</button><StatusPill tone="blue">Human oversight</StatusPill></div><div className="recommendation-list">{recs.map((rec, index) => <Card className="recommendation-card" key={rec.title}><div className={`rec-number ${rec.tone}`}>0{index + 1}</div><div className="rec-content"><div className="rec-head"><div><span className="card-kicker">Recommendation {index + 1}</span><h3>{rec.title}</h3></div><StatusPill tone={rec.tone === "amber" ? "amber" : rec.tone === "blue" ? "blue" : "teal"}>{rec.confidence} confidence</StatusPill></div><p className="rec-reason"><strong>Why:</strong> {rec.reason}</p><div className="rec-evidence"><div><span>Supporting data</span><strong>{rec.evidence}</strong></div><div><span>Expected direction</span><strong>{rec.impact}</strong></div><div><span>Assumption</span><strong>Validate with local context</strong></div></div></div><ChevronRight size={18} className="rec-chevron" /></Card>)}</div><Card className="recommendation-footer"><Info size={16} /><span>Do not treat modeled savings as realized savings. Measure before and after any intervention to validate impact.</span></Card></>;
}

function ActionCenterPage({ go }: { go: (section: Section) => void }) {
  const items = [
    { priority: "HIGH PRIORITY", tone: "red", title: "Computer Labs — unusual consumption", detail: "Observed 145 kWh vs modeled 80–100 kWh range at 14:00.", view: "anomalies" as Section, analyze: "monitoring" as Section, simulate: "whatif" as Section },
    { priority: "MEDIUM", tone: "amber", title: "Hostel — predicted evening peak", detail: "Tomorrow’s model shows a higher demand window around 18:00–20:00.", view: "peak" as Section, analyze: "predictions" as Section, simulate: "whatif" as Section },
    { priority: "OPPORTUNITY", tone: "teal", title: "Academic Block — LED scenario", detail: "The scenario lab can test a modeled lighting reduction before any decision.", view: "twin" as Section, analyze: "recommendations" as Section, simulate: "whatif" as Section },
  ];
  return <><SectionTitle eyebrow="Decision support · action center" title="What needs attention next?" copy="A prioritized review queue for facility and sustainability teams. Items are not physically resolved until an authorized person verifies them." action={<StatusPill tone="amber">3 open items</StatusPill>} /><div className="action-center-banner"><ClipboardCheck size={18} /><div><strong>Human verification required</strong><span>Use View, Analyze, and Simulate to investigate. Verify / Resolve only records your review state; it does not control equipment.</span></div></div><div className="action-list-cards">{items.map((item) => <Card className="action-center-card" key={item.title}><div className={`action-priority ${item.tone}`}>{item.priority}</div><div className="action-center-main"><h3>{item.title}</h3><p>{item.detail}</p><div className="action-buttons"><button className="button button-secondary" onClick={() => go(item.view)}>View <ChevronRight size={14} /></button><button className="button button-secondary" onClick={() => go(item.analyze)}>Analyze <LineChart size={14} /></button><button className="button button-secondary" onClick={() => go(item.simulate)}>Simulate <SlidersHorizontal size={14} /></button><button className="button button-dark" onClick={() => toast.success("Review recorded", { description: "This item is marked as reviewed in the demo workspace; no physical system was changed." })}><Check size={14} /> Verify / Resolve</button></div></div></Card>)}</div></>;
}

function AssistantPage({ messages, input, setInput, ask }: { messages: { from: "user" | "ai"; text: string; type?: string }[]; input: string; setInput: (value: string) => void; ask: () => void }) {
  const suggestions = ["Which building consumed the most energy?", "What is tomorrow's predicted consumption?", "Why was Lab 2 flagged?", "How does this support SDG 7?"];
  return <><SectionTitle eyebrow="EnergyAI assistant · local knowledge base" title="Ask the campus questions." copy="A conversational layer over the demo data, model outputs, scenario engine, and project methodology." action={<StatusPill tone="teal">No API key required</StatusPill>} /><div className="assistant-layout"><Card className="assistant-chat"><div className="assistant-chat-head"><div className="ai-orb"><MessageCircle size={18} /></div><div><strong>EnergyAI Assistant</strong><span>Grounded in the current workspace</span></div><span className="assistant-online"><span className="live-dot" /> Ready</span></div><div className="message-list">{messages.map((message, index) => <div className={`message-row ${message.from}`} key={`${message.text}-${index}`}><div className="message-avatar">{message.from === "ai" ? <Sparkles size={14} /> : "FM"}</div><div className="message-bubble"><p>{message.text}</p>{message.type && <span className="message-type">{message.type}</span>}</div></div>)}</div><div className="suggestions">{suggestions.map((suggestion) => <button key={suggestion} onClick={() => setInput(suggestion)}>{suggestion}</button>)}</div><div className="assistant-input"><input value={input} onChange={(e) => setInput(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") ask(); }} placeholder="Ask about the energy picture…" /><button className="button button-primary" onClick={ask}><ArrowUpRight size={16} /></button></div><small className="assistant-disclaimer">Answers distinguish data-derived facts, model predictions, scenario estimates, and knowledge-base explanations.</small></Card><div className="assistant-side"><Card><span className="card-kicker">Knowledge base</span><h3>What EnergyAI can explain</h3><div className="knowledge-list"><div><Database size={15} /><span>Current campus and building data</span></div><div><LineChart size={15} /><span>Prediction inputs and uncertainty</span></div><div><AlertTriangle size={15} /><span>Anomaly evidence and possible factors</span></div><div><Leaf size={15} /><span>SDG 7 and sustainability concepts</span></div></div></Card><Card className="assistant-guardrail"><ShieldCheck size={17} /><strong>Grounding guardrail</strong><p>If the workspace does not contain enough data, the assistant will say so instead of inventing a number.</p></Card></div></div></>;
}

function ImpactPage({ baseline, simulatedEnergy, simulatedCost, simulatedCo2, settings, solarContribution }: { baseline: number; simulatedEnergy: number; simulatedCost: number; simulatedCo2: number; settings: { tariff: number; emissions: number }; solarContribution: number }) {
  const savings = baseline - simulatedEnergy;
  return <><SectionTitle eyebrow="Sustainability impact" title="Make potential impact legible." copy="A transparent view of the current scenario estimate and its relationship to SDG 7. This is not a claim of achieved impact." action={<StatusPill tone="amber">Potential contribution</StatusPill>} /><div className="impact-hero"><div><span className="card-kicker">Selected scenario estimate</span><h2>Efficiency + renewable pathways, in one view.</h2><p>Use the current What-If assumptions to understand the direction of modeled energy, cost, emissions, and renewable contribution.</p></div><div className="impact-total"><span>Estimated daily reduction</span><strong>{num(savings)} <small>kWh</small></strong><span>vs current campus baseline</span></div></div><div className="impact-grid"><Card className="impact-card teal-impact"><div className="impact-icon"><Zap size={18} /></div><span>Energy</span><strong>−{num(savings)} kWh</strong><small>Estimated energy reduction · {pct((savings / baseline) * 100)}</small><div className="impact-bar"><span style={{ width: `${Math.min(100, (savings / baseline) * 100 * 2)}%` }} /></div></Card><Card className="impact-card blue-impact"><div className="impact-icon"><BatteryCharging size={18} /></div><span>Cost</span><strong>−{money(baseline * settings.tariff / 100 - simulatedCost)}</strong><small>Estimated difference at ₹{settings.tariff}/kWh</small><div className="impact-bar"><span style={{ width: "46%" }} /></div></Card><Card className="impact-card slate-impact"><div className="impact-icon"><CloudSun size={18} /></div><span>Climate</span><strong>−{(baseline * settings.emissions / 1000 - simulatedCo2).toFixed(2)} t</strong><small>Estimated CO₂e difference · factor-dependent</small><div className="impact-bar"><span style={{ width: "40%" }} /></div></Card><Card className="impact-card sun-impact"><div className="impact-icon"><SunMedium size={18} /></div><span>Renewable energy</span><strong>{pct(solarContribution)}</strong><small>Modeled contribution in current scenario</small><div className="impact-bar"><span style={{ width: `${solarContribution}%` }} /></div></Card></div><div className="sdg-grid"><Card><div className="sdg-number">7</div><div><span className="card-kicker">Primary alignment</span><h3>Affordable and Clean Energy</h3><p>Supports SDG 7 by helping institutions understand demand, explore energy efficiency, and investigate renewable-energy options through transparent decision support.</p></div></Card><Card><div className="sdg-secondary"><span>Optional alignment</span><div><StatusPill tone="blue">SDG 11</StatusPill><StatusPill tone="teal">SDG 12</StatusPill><StatusPill tone="slate">SDG 13</StatusPill></div></div><p>Potential contribution to sustainable communities, responsible consumption, and climate action — not a claim that the application has achieved any target.</p></Card></div></>;
}

function ReportsPage({ downloadReport, go }: { downloadReport: () => void; go: (section: Section) => void }) {
  return <><SectionTitle eyebrow="Automatic sustainability report" title="Turn the workspace into a brief." copy="Generate a concise report that keeps the data mode, assumptions, model outputs, and limitations visible." action={<button className="button button-primary" onClick={downloadReport}><Download size={16} /> Download report</button>} /><div className="report-layout"><Card className="report-preview"><div className="report-cover"><div className="report-brand"><div className="brand-mark"><Zap size={16} fill="currentColor" /></div><strong>CampusEnergy Twin</strong></div><span>Decision-support brief / 01</span><h2>Campus energy, made legible.</h2><p>Monitoring · prediction · anomaly review · scenario planning</p><div className="report-cover-meta"><span>Northbridge Institute</span><span>Demo / synthetic dataset</span><span>18 September 2026</span></div></div><div className="report-outline"><div><FileCheck2 size={16} /><span>Campus overview & reporting period</span><Check size={15} /></div><div><BarChart3 size={16} /><span>Building-level analysis & peak demand</span><Check size={15} /></div><div><AlertTriangle size={16} /><span>Anomaly summary & human review</span><Check size={15} /></div><div><Sparkles size={16} /><span>AI predictions & recommendations</span><Check size={15} /></div><div><SlidersHorizontal size={16} /><span>Scenario estimates & assumptions</span><Check size={15} /></div><div><ShieldCheck size={16} /><span>Responsible AI & SDG 7 alignment</span><Check size={15} /></div></div></Card><div className="report-side"><Card><span className="card-kicker">Report status</span><h3>Ready to export</h3><div className="report-status"><span className="live-dot" /> All required sections populated</div><div className="report-facts"><div><span>Data source</span><strong>Demo synthetic data</strong></div><div><span>Model</span><strong>Local baseline</strong></div><div><span>Assumptions</span><strong>Tariff + emissions factor</strong></div></div><button className="button button-dark full-width" onClick={downloadReport}><Download size={16} /> Export text report</button></Card><Card className="report-tip"><FileBarChart size={17} /><div><strong>Before sharing</strong><p>Replace demo data with a validated real dataset and confirm local assumptions if this brief is used outside the demo environment.</p></div></Card><button className="button button-ghost full-width" onClick={() => go("responsible")}><ShieldCheck size={16} /> Review responsible AI notes</button></div></div></>;
}

function DataSourceOptions({ activeSource, switchSource, csvResult, meterRunning, toggleMeter, meterReading }: { activeSource: DataSource; switchSource: (source: DataSource) => void; csvResult: { valid: boolean } | null; meterRunning: boolean; toggleMeter: () => void; meterReading: { building: string; meter: string; energy: number; power: number; voltage: number; current: number; timestamp: string } }) {
  return <Card className="data-source-options"><div className="card-heading"><div><span className="card-kicker">Data Source</span><h3>Choose how campus data enters the app.</h3></div><StatusPill tone={activeSource === "demo" ? "amber" : "teal"}>{sourceLabels[activeSource]}</StatusPill></div><div className="data-source-option-grid"><button className={`data-source-option ${activeSource === "uploaded" ? "active" : ""}`} onClick={() => switchSource("uploaded")} disabled={!csvResult?.valid}><Upload size={17} /><span><strong>CSV Upload</strong><small>{csvResult?.valid ? "Validated data ready to import" : "Upload and validate a CSV first"}</small></span><ChevronRight size={15} /></button><button className={`data-source-option ${activeSource === "demo" ? "active" : ""}`} onClick={() => switchSource("demo")}><Database size={17} /><span><strong>Demo / Synthetic Data</strong><small>Use the existing synthetic dataset</small></span><ChevronRight size={15} /></button><button className={`data-source-option ${activeSource === "simulated" ? "active" : ""}`} onClick={toggleMeter}><Activity size={17} /><span><strong>Smart Meter / IoT / API</strong><small>{meterRunning ? "SIMULATED • SMART METER" : "Start automatic simulation"}</small></span>{meterRunning ? <StatusPill tone="teal">Live simulation</StatusPill> : <ChevronRight size={15} />}</button></div>{meterRunning && <div className="data-source-reading"><span>SIMULATED • SMART METER</span><strong>{meterReading.building} · {meterReading.meter}</strong><span>{meterReading.timestamp} · {meterReading.energy} kWh · {meterReading.power} kW · {meterReading.voltage} V · {meterReading.current} A</span></div>}</Card>;
}

function DataPage({ csvResult, csvProgress = 0, handleCsv, activeSource, switchSource, meterRunning, toggleMeter, meterReading }: { csvResult: { name: string; rows: number; buildings: number; missing: number; invalid: number; duplicates: number; valid: boolean; dateRange: string; totalEnergy: number; errors: { row: number; message: string; raw: string }[] } | null; csvProgress?: number; handleCsv: (event: ChangeEvent<HTMLInputElement>) => void; activeSource: DataSource; switchSource: (source: DataSource) => void; meterRunning: boolean; toggleMeter: () => void; meterReading: { building: string; meter: string; energy: number; power: number; voltage: number; current: number; timestamp: string } }) {
  return <><SectionTitle eyebrow="Data management" title="Bring your own energy data." copy="Upload a CSV to replace the demo context. The validation preview runs in your browser and does not require personal information." action={<label className="button button-primary upload-button"><Upload size={16} /> Upload CSV<input type="file" accept=".csv,text/csv" onChange={handleCsv} /></label>} /><DataSourceOptions activeSource={activeSource} switchSource={switchSource} csvResult={csvResult} meterRunning={meterRunning} toggleMeter={toggleMeter} meterReading={meterReading} />{csvProgress > 0 && <div className="csv-progress data-progress"><div className="csv-progress-top"><span>{csvProgress < 100 ? "Uploading and validating…" : "Validation complete"}</span><strong>{csvProgress}%</strong></div><div className="csv-progress-track"><span style={{ width: `${csvProgress}%` }} /></div></div>}<div className="data-mode-banner"><div className="data-mode-icon"><Database size={17} /></div><div><strong>Current source: {sourceLabels[activeSource]}</strong><span>{activeSource === "uploaded" ? "Persisted campus readings feed the shared analytics workspace." : activeSource === "simulated" ? "Generated readings feed the shared prototype source state." : "The existing synthetic dataset remains active."}</span></div><StatusPill tone={activeSource === "demo" ? "amber" : "teal"}>{activeSource === "demo" ? "Synthetic" : "Active"}</StatusPill></div><div className="data-grid"><Card className="data-preview"><div className="card-heading"><div><span className="card-kicker">Dataset preview</span><h3>{csvResult ? csvResult.name : "demo_energy_sample.csv"}</h3></div><span className="table-caption">{csvResult ? `${csvResult.rows} records · ${csvResult.buildings} buildings` : "42 days · 6 buildings"}</span></div><div className="table-scroll"><table><thead><tr><th>date</th><th>time</th><th>building</th><th>occupancy</th><th>energy_consumption</th></tr></thead><tbody>{[["2026-09-11", "14:00", "Computer Labs", "58", "145"], ["2026-09-11", "15:00", "Academic Block", "72", "88"], ["2026-09-11", "16:00", "Hostel", "81", "96"], ["2026-09-12", "14:00", "Library", "46", "42"], ["2026-09-12", "15:00", "Canteen", "39", "28"]].map((row) => <tr key={row.join("-")}>{row.map((cell) => <td key={cell}>{cell}</td>)}</tr>)}</tbody></table></div><div className="preview-foot"><span><Info size={13} /> Showing a small preview of the active demo dataset.</span><button className="small-link">Download sample <Download size={13} /></button></div></Card><div className="validation-stack"><Card><div className="card-heading"><div><span className="card-kicker">Validation checklist</span><h3>{csvResult ? "Upload result" : "Ready for validation"}</h3></div><FileCheck2 size={18} className="muted-icon" /></div><div className="validation-list"><div><span className="validation-check"><Check size={13} /></span><span>Required columns</span><strong className="teal-text">date · building · energy</strong></div><div><span className={`validation-check ${csvResult?.invalid ? "warning" : ""}`}>{csvResult?.invalid ? <AlertTriangle size={13} /> : <Check size={13} />}</span><span>Invalid values</span><strong className={csvResult?.invalid ? "amber-text" : "teal-text"}>{csvResult ? csvResult.invalid : "none"}</strong></div><div><span className="validation-check"><Check size={13} /></span><span>Numeric values</span><strong className="teal-text">checked</strong></div><div><span className={`validation-check ${csvResult?.duplicates ? "warning" : ""}`}>{csvResult?.duplicates ? <AlertTriangle size={13} /> : <Check size={13} />}</span><span>Duplicate rows</span><strong className={csvResult?.duplicates ? "amber-text" : "teal-text"}>{csvResult ? csvResult.duplicates : "none"}</strong></div><div><span className={`validation-check ${csvResult?.missing ? "warning" : ""}`}>{csvResult?.missing ? <AlertTriangle size={13} /> : <Check size={13} />}</span><span>Missing required columns</span><strong className={csvResult?.missing ? "amber-text" : "teal-text"}>{csvResult ? csvResult.missing : "none"}</strong></div></div>{csvResult && <><div className={`upload-result ${csvResult.valid ? "valid" : "invalid"}`}><span>{csvResult.valid ? <Check size={15} /> : <AlertTriangle size={15} />}</span>{csvResult.valid ? `Dataset is ready for analysis · ${csvResult.dateRange}` : `Dataset needs attention · ${csvResult.errors.length} detailed issue(s)`}</div>{csvResult.errors.length > 0 && <div className="csv-error-panel"><div className="card-heading"><div><span className="card-kicker">Invalid row details</span><h3>Fix these records before import</h3></div><AlertTriangle size={17} className="muted-icon" /></div>{csvResult.errors.slice(0, 10).map((error) => <div className="csv-error-row" key={`${error.row}-${error.message}`}><strong>Row {error.row}</strong><span>{error.message}</span><code>{error.raw}</code></div>)}{csvResult.errors.length > 10 && <small>Showing 10 of {csvResult.errors.length} issues. Correct the source CSV and upload again.</small>}</div>}</>}</Card><Card className="column-card"><span className="card-kicker">Recommended columns</span><h3>Optional context makes the model stronger.</h3><div className="column-tags"><span>date</span><span>time</span><span>building</span><span>room</span><span>occupancy</span><span>ac_usage</span><span>lighting_usage</span><span>equipment_usage</span><span>temperature</span><span>energy_consumption</span></div><p>Occupancy should be aggregated. Do not upload names, phone numbers, facial data, or other personal information.</p></Card></div></div></>;
}

function ResponsiblePage() {
  const items = [
    ["Fairness", "Recommendations should not unfairly prioritize one building or group because its data is more complete or easier to measure."],
    ["Transparency", "The workspace shows what data was used, how the model is framed, which assumptions are active, and why an anomaly or recommendation appeared."],
    ["Privacy", "The core workflow does not require names, phone numbers, facial data, or other identifying information. Prefer aggregated occupancy."],
    ["Human oversight", "AI outputs are decision-support suggestions. Facility managers and administrators remain responsible for final decisions."],
    ["Accuracy & uncertainty", "Predictions and simulations are estimates. Uncertainty grows when data is sparse, incomplete, or not representative."],
    ["Data quality", "Poor or incomplete data can lead to poor predictions. Validate dates, numeric values, missing values, duplicates, and building names before analysis."],
  ];
  return <><SectionTitle eyebrow="Governance · responsible AI" title="Designing for trust, not certainty." copy="The system is intentionally explicit about what it knows, what it estimates, and where a human must verify." action={<StatusPill tone="teal">Required section</StatusPill>} /><div className="responsible-grid">{items.map(([title, text], index) => <Card key={title} className="responsible-card"><div className={`responsible-number number-${index + 1}`}>0{index + 1}</div><div><h3>{title}</h3><p>{text}</p></div></Card>)}</div><Card className="limitations-card"><div className="limitations-title"><ShieldCheck size={19} /><div><span className="card-kicker">Project limitations</span><h3>What this prototype does not do</h3></div></div><div className="limitations-list"><span>Does not directly control electrical equipment</span><span>Does not guarantee savings or carbon reduction</span><span>Does not replace meter validation</span><span>Does not infer exact physical causes</span><span>Does not claim official SDG achievement</span><span>Does not require personal information</span></div></Card></>;
}

function AboutPage({ go }: { go: (section: Section) => void }) {
  return <><SectionTitle eyebrow="About the project" title="A campus energy twin for better questions." copy="Built as an AI + sustainability student project — practical enough to explore, transparent enough to discuss." action={<button className="button button-secondary" onClick={() => go("responsible")}><ShieldCheck size={16} /> Responsible AI</button>} /><div className="about-hero"><div className="about-mark"><Zap size={35} fill="currentColor" /></div><div><span className="card-kicker">CampusEnergy Twin</span><h2>AI-powered digital twin for sustainable campus energy management.</h2><p>CampusEnergy Twin helps educational institutions monitor energy consumption, predict demand, detect unusual usage patterns, simulate efficiency and renewable-energy scenarios, and understand potential sustainability impacts.</p></div></div><div className="about-grid"><Card><span className="card-kicker">Primary SDG</span><div className="sdg-lockup"><div className="sdg-number">7</div><div><h3>Affordable and Clean Energy</h3><p>Supports data-informed investigation of energy efficiency and renewable pathways.</p></div></div></Card><Card><span className="card-kicker">AI components</span><div className="about-tags"><span>Machine learning</span><span>Anomaly detection</span><span>Forecasting</span><span>Pattern analysis</span><span>Recommendation engine</span><span>Conversational AI</span><span>Lightweight knowledge base</span></div></Card><Card><span className="card-kicker">Intended users</span><div className="user-list"><span><Building2 size={15} /> Educational institutions</span><span><Settings2 size={15} /> Facility managers</span><span><UserRound size={15} /> Administrators</span><span><Leaf size={15} /> Sustainability teams</span></div></Card><Card><span className="card-kicker">Project context</span><h3>1M1B AI for Sustainability Virtual Internship</h3><p>In collaboration with IBM SkillsBuild & AICTE. This product does not claim endorsement beyond the stated internship context.</p></Card></div></>;
}

function SettingsPage({ settings, setSettings, readOnly }: { settings: { campus: string; tariff: number; emissions: number }; setSettings: (settings: { campus: string; tariff: number; emissions: number }) => void; readOnly: boolean }) {
  return <><SectionTitle eyebrow="Workspace settings" title="Make assumptions explicit." copy="These configurable values feed cost and CO₂e estimates across the workspace." action={<StatusPill tone="blue">Local settings</StatusPill>} /><div className="settings-layout"><Card className="settings-card"><div className="card-heading"><div><span className="card-kicker">Campus profile</span><h3>Calculation settings</h3></div><Settings2 size={19} className="muted-icon" /></div><div className="form-field"><label>Campus name</label><input disabled={readOnly} value={settings.campus} onChange={(e) => setSettings({ ...settings, campus: e.target.value })} /><small>Shown in the report and campus twin.</small></div><div className="form-field"><label>Electricity tariff per kWh</label><div className="input-with-suffix"><span>₹</span><input disabled={readOnly} type="number" value={settings.tariff} min={0} step={0.5} onChange={(e) => setSettings({ ...settings, tariff: Number(e.target.value) || 0 })} /><span>/ kWh</span></div><small>Estimated cost = energy consumption × tariff.</small></div><div className="form-field"><label>Emissions factor</label><div className="input-with-suffix"><input disabled={readOnly} type="number" value={settings.emissions} min={0} step={0.01} onChange={(e) => setSettings({ ...settings, emissions: Number(e.target.value) || 0 })} /><span>kgCO₂e / kWh</span></div><small>Factor varies by grid mix, methodology, and location.</small></div><button className="button button-primary" disabled={readOnly} onClick={() => toast.success("Settings saved locally")}>{readOnly ? "Read-only evaluator mode" : "Save settings"} <Check size={16} /></button></Card><Card className="settings-explain"><div className="settings-formula"><span>Estimated Cost</span><strong>Energy × Tariff</strong><code>5,810 × ₹{settings.tariff} / 1000</code></div><div className="settings-formula"><span>Estimated CO₂e</span><strong>Energy × Emissions factor</strong><code>5,810 × {settings.emissions} / 1000</code></div><div className="settings-warning"><Info size={15} /><span>These are assumptions, not local utility facts. Confirm values with your institution before using the outputs operationally.</span></div></Card></div></>;
}

export default App;

